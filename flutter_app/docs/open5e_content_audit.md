# Open5e Pack Content Audit — Checklist & Roadmap

**Status:** measurement done, remediation not started.
**Numbers regenerated by:** `dart run tool/open5e_import/bin/audit_packs.dart`
**Measured:** 2026-07-29, against the 19 assets in `assets/open5e_packs/`.

---

## 0. Start here

**This file is the single entry point for fixing official-pack content.** It is
self-contained: everything needed to pick up the work is below, and the links out
are optional depth, never prerequisites. Open a session with this file alone.

**The one-line problem.** The bundled Open5e packs ship the right entities with
mostly empty fields — most sharply, 101 subclasses that attach to the correct
parent class and grant nothing.

**Next action right now:** §5 phase **A0** — restore the source snapshot. It blocks
every other phase.

**Working loop.** Pick the topmost unchecked box in §5 → make the change in the
importer → `dart run tool/open5e_import/bin/build_packs.dart` → re-run
`audit_packs.dart` → update the affected rows in §4 and tick the box.
Keep §2 and §4 in sync with the tool; never hand-edit a number.

### Map of the territory

| Where | What |
|---|---|
| `tool/open5e_import/bin/build_packs.dart` | entry point; decides which fixture files are loaded per document — **most `L` fixes land here** |
| `tool/open5e_import/sources.dart` | document discovery, `_mappedFiles`, licences, the `isSrdOverlap` skip |
| `tool/open5e_import/loaders.dart` | Django-fixture reader + `groupBy`/`byPk` for joining child tables |
| `tool/open5e_import/mappers/chargen.dart` | class / subclass / species / background / feat — **most `M` fixes land here** |
| `tool/open5e_import/mappers/{monster,spell,item}.dart` | the other three mappers |
| `tool/open5e_import/normalize.dart` | Tier-0 vocabulary normalisation + `unmapped_report.json` |
| `tool/open5e_import/refgraph.dart` | `PackBuilder`, uuidv5 ids, two-pass `_ref` resolution |
| `tool/open5e_import/bin/audit_packs.dart` | the census behind §2 and §4 |
| `assets/open5e_packs/*.pkg.json` | the 19 shipped assets this audit measures |
| `lib/domain/entities/schema/builtin/content.dart` | the target field definitions (what each category declares) |
| `lib/domain/services/character_resolver.dart` | what actually *reads* a field — a field nobody reads is not a fix |

### Scope

**In scope:** the 19 bundled Open5e packs in `assets/open5e_packs/` and the offline
importer that produces them.

**Out of scope:** the hand-authored built-in SRD 5.2.1 pack
(`lib/domain/entities/schema/builtin/srd_core/`). It is a separate content source
with its own tests (`feature_grant_edge_test`, `grant_contract_test`) and is
already wired. Do not conflate the two: when a bundled subclass points at
"Paladin", that Paladin comes from the built-in pack.

**Optional depth:** `open5e_import_roadmap.md` (how the importer was originally
built, licensing, phases P0–P6). Read it only if the map above is not enough —
parts of it are superseded by this file and marked as such.

### The target architecture — read before writing a mapper fix

Every fix in §5 writes into the **named-field grant system** that replaced the
effect DSL on 2026-07-28. `vault/20-Systems/Grant-Resolution.md` is the authority
on it; `CharacterResolver.grantFieldKeys` is the closed 41-key contract. Four
rules constrain what a correct fix looks like:

1. **One mechanic, one field.** There is no `kind`/`predicate`/`scales_with` row
   language any more — `FieldType.featEffectList` and `grantedModifiers` are
   deleted. Never invent a key; if a mechanic has no field, that is a schema
   change with four guards to satisfy (`grant_contract_test`,
   `grant_field_isolation_test`), not an importer change.
2. **Class, Subclass and Background deliberately do NOT carry the grant block.**
   Seeing `subclass` at 1-of-8 filled and "fixing" it by handing it the block
   would recreate exactly the two-fields-one-mechanic ambiguity the removal was
   for — and `grant_contract_test` fails on it. Their grants go in their own
   typed fields (`saving_throw_refs`, `armor_training_refs`, `granted_skill_refs`,
   …), and everything level-gated goes on a `features` row.
3. **Level-gated mechanics live on a Feat card, referenced from the `features`
   row** — `{level, name, description, granted_feat_refs, granted_trait_refs, …}`.
   The class card names *which* feat arrives at *which* level; the Feat card
   carries the mechanics via the grant block. This is the forward edge that
   replaced `feat.auto_granted_by`. Scaling is a `{lvl: value}` table
   (`extra_attack_count_by_level`), never a formula.
4. **Nothing is silently dropped.** Rules the engine cannot pre-compute
   (advantage, rerolls, roll-time riders, timing) belong in `mechanical_notes`,
   one rule per line — they render on the sheet under "Other Effects". Today
   `mechanical_notes` is **0% across every bundled category**, which means every
   §4 row below reading "stays in prose" is currently invisible to the player.
   Routing them there is the cheapest real win in this audit (§5 B5).

---

## 1. Why this exists

The bundled Open5e packs ship the right **entities** but mostly empty **fields**.
A player installing *Tome of Heroes* gets 76 subclasses that attach to the correct
parent class and then grant nothing — they carry `description` and
`parent_class_ref` and not one other field.

The wiring is not the problem. All 97 `parent_class_ref` softRefs on the bundled
subclasses resolve by name against the built-in SRD classes (Cleric ×11, Wizard ×11,
Monk ×9, …, zero misses). The payload behind them is what is missing.

This document splits the gap into the two questions that have different answers and
different fixes:

- **Stage A — Source.** Is the information in the Open5e JSON at all?
  → If not, re-fetch or re-export from the API (§3).
- **Stage B — Application.** It *is* in the JSON; did the importer read it and write
  it onto the right app field?
  → If not, fix the mapper and rebuild the packs (§4).

### Cause codes

Every row below carries one. They are not interchangeable — `S` is upstream work,
`L`/`M` are ours, `P`/`N` are closed.

| Code | Meaning | Where the fix lands |
|:--:|---|---|
| `S` | Not in the source dump for the documents we ship | re-fetch, or accept as permanent |
| `L` | In the dump, in a fixture file the importer never **loads** | `bin/build_packs.dart` + `sources.dart` |
| `M` | File loaded, field not **mapped** or mapped wrong | `mappers/*.dart` |
| `P` | Deliberately not mapped, decision on record | none — link the decision |
| `N` | App field has no Open5e counterpart | none — permanently empty |

### Status markers

| | Meaning |
|:--:|---|
| ✅ | Every entity carries it |
| 🟡 | Some do — the gap is the finding |
| 🔴 | Never written |
| ⚠ | Filled, but with the **same constant on every row** — the mapper wrote a default instead of reading the source. A 100% column with ⚠ is not coverage. |
| ⛔ | Policy: intentionally empty |
| ⚪ | No source field exists |

---

## 2. What ships today

"Filled" = declared fields at least one entity carries a value for.

| Category | Entities | Packs | Declared fields | Filled |
|---|--:|--:|--:|--:|
| `class` | 2 | 2 | 30 | 7 |
| `subclass` | 101 | 5 | 8 | **1** |
| `species` | 11 | 1 | 48 | 9 |
| `subspecies` | 30 | 2 | 48 | 10 |
| `background` | 53 | 6 | 11 | 4 |
| `feat` | 73 | 3 | 60 | 13 |
| `spell` | 1297 | 8 | 25 | 18 |
| `magic-item` | 1063 | 1 | 64 | 8 |
| `monster` | 2885 | 8 | 40 | 35 |
| `creature-action` | 8615 | 8 | 19 | 13 |
| `trait` | 6423 | 8 | 46 | 3 |
| `adventuring-gear` | 159 | 6 | 8 | 4 |

Every chargen category also carries a `description` key in `attributes` that the
schema does not declare (`feat` additionally carries `prereq_clauses`) — see the
tool's "Undeclared keys" footer.

Only 2 base classes ship because the 24 others live in `srd-2014` / `srd-2024`,
which `build_packs.dart` skips as SRD overlaps (the built-in pack ships them).
The Open5e API holds 151 CharacterClass rows total; 103 of them ship here and 48
are in the skipped SRD documents — **no entity is being lost**. The loss is entirely
at field level.

---

## 3. Stage A — source snapshot

### A0. The snapshot is not on this machine — blocking

`open5e-api-staging/` is gitignored and absent. `build_packs.dart` defaults
`--data` to `../open5e-api-staging/data` (i.e. `<repo-root>/open5e-api-staging/`,
one level above `flutter_app/`). **Nothing in Stage B can be fixed or re-verified
until it is restored.**

The fixtures are the `data/` directory of the Open5e API repo's `staging` branch —
that branch name is where the `-staging` suffix comes from:

```sh
cd /path/to/dungeon-master-tool          # repo root, NOT flutter_app
git clone --branch staging --depth 1 \
    https://github.com/open5e/open5e-api.git open5e-api-staging
ls open5e-api-staging/data/v2            # → en-publishing green-ronin kobold-press
                                         #   open5e somanyrobots wizards-of-the-coast
```

`open5e-api-staging/` is already in `.gitignore`, so it will not be committed.

- [ ] Clone as above
- [ ] Record the checked-out commit and pass it as `--rev` (the hardcoded default is
      `staging-2026-05-31`; if the clone is newer, the rebuild diff below mixes
      upstream content changes with our mapper changes — pin or note it)
- [ ] Rebuild and diff against the shipped assets — a byte-identical rebuild proves
      the snapshot matches what was shipped, and any diff is itself a finding

> There is already evidence the shipped assets **lag the mapper**: `mapSpecies`
> writes `granted_senses` as `[{sense_ref: …}]`, but every shipped payload is a
> bare `[{_lookup: sense, name: Darkvision}]`. So the diff above will not be
> empty, and the delta is a free list of fixes that were made but never rebuilt.
> (`tool/migrate_pack_assets.dart` also rewrites assets in place, so a diff needs
> to account for it.)

### A1. Fixture files: loaded vs. present upstream

Verified against `open5e/open5e-api` for `wizards-of-the-coast/srd-2024` (34 files),
`kobold-press/toh` (12), `en-publishing/a5e-ag` (16).

**Loaded today** — `sources.dart` `_mappedFiles` + the child files
`build_packs.dart` opens opportunistically:

`Document` · `Creature` · `CreatureAction` · `CreatureActionAttack` · `CreatureTrait` ·
`Spell` · `MagicItem` · `CharacterClass` · `ClassFeature` · `Species` · `SpeciesTrait` ·
`Background` · `BackgroundBenefit` · `Feat` · `FeatBenefit`

**Present upstream and never loaded:**

| File | Holds | Blocks | Decision |
|---|---|---|:--:|
| `ClassFeatureItem.json` | `level` (0–20), `detail`, `column_value`, FK → `ClassFeature` | `class.features`, `subclass.features`, `subclass.granted_at_level`, all three class level tables | [ ] load |
| `SpellCastingOption.json` | per-casting-option `damage_roll`, `target_count`, `duration`, `range`, `shape_size` | `spell.at_higher_levels_text` | [ ] load |
| `Item.json`, `Weapon.json`, `Armor.json` | mundane equipment | the 159 synthesised gear stubs | [ ] decide |
| `WeaponProperty.json`, `WeaponPropertyAssignment.json` | weapon properties | — | [ ] decide |
| `ItemCategory.json` | item taxonomy | `magic-item.body_slot_ref` | [ ] decide |
| `Rule.json`, `RuleSet.json` | rules/condition prose | — | [ ] decide |
| `CrossReference.json` | entity↔entity links | could seed refs we currently name-match | [ ] decide |
| `*Description.json` (Ability/Skill/Condition/DamageType/Alignment/CreatureType) | Tier-0 flavour text | — | [ ] decide |

> **`ClassFeatureItem.json` is the headline.** It exists in **every** document that
> ships classes. [`mappers/chargen.dart`](../tool/open5e_import/mappers/chargen.dart)
> currently justifies the empty `features` table with *"Open5e `ClassFeature` rows
> have no level field"* — true of `ClassFeature`, but the level lives one table down
> in `ClassFeatureItem.level`. This is an `L`, not an `S`. Everything in §4.1 and
> §4.2 unblocks from here.

### A2. Document coverage

- [ ] `open5e/open5e-2024` exists upstream but produces no pack — confirm whether it
      has mappable content or is correctly skipped
- [ ] `open5e_import_roadmap.md` claims 22 packages; 19 ship. Reconcile.
- [ ] `srd-2014` / `srd-2024` are skipped as SRD overlaps. They are also the only
      documents carrying SRD-2024 origin feats and ASI distributions — confirm the
      skip is still right now that those fields are required (see §4.5).

---

## 4. Stage B — per-category field state

Rows are grouped by the schema's own `FieldGroup`s, so a row maps 1:1 to a section
of the card a DM sees. Full generated tables: `audit_packs.dart --markdown`.

### 4.1 `class` — 2 entities

| | Field | Group | Req | Fill | Cause | Source |
|:--:|---|---|:--:|--:|:--:|---|
| 🔴 | `primary_ability_ref` | Core Traits | **yes** | 0% | `S` | `CharacterClass.primary_abilities` — mapped, but empty in both shipped docs |
| 🔴 | `secondary_ability_ref` | Core Traits | | 0% | ⚪ | no counterpart |
| ✅ | `hit_die` | Core Traits | **yes** | 100% | | `hit_dice` |
| ✅ | `saving_throw_refs` | Core Traits | **yes** | 100% | | `saving_throws` |
| ✅ | `skill_proficiency_choice_count` / `_options` | Progression | | 100% | | parsed from the Proficiencies feature prose |
| ✅ | `weapon_proficiency_categories`, `armor_training_refs` | Progression | | 100% | | same prose parse |
| 🔴 | `weapon_proficiency_specifics` | Progression | | 0% | `M` | Proficiencies prose names specific weapons |
| 🔴 | `tool_proficiency_count` / `_options` | Progression | | 0% | `M` | `**Tools:**` line never parsed |
| 🔴 | `granted_tool_refs`, `granted_languages` | Progression | | 0% | `M` | Druidic / Thieves' Tools are in feature prose |
| 🔴 | `default_inventory_refs`, `equipment_choice_groups`, `starting_gold_dice` | Progression | | 0% | `M` | starting-equipment `ClassFeature` never parsed (`mapBackgrounds` already has the A/B parser to reuse) |
| 🔴 | `multiclass_prereq_*`, `multiclass_requirements`, `multiclass_granted_proficiencies` | Progression | | 0% | `M` | in prose |
| 🔴 | `l1_order_feat_category`, `weapon_mastery_filter`, `complexity` | Progression | | 0% | ⚪ | app-only concepts |
| 🔴 | `casting_ability_ref`, `spellcasting_focus_ref` | Spellcasting | | 0% | `M` | Spellcasting feature prose |
| ✅ | `caster_kind` | Spellcasting | **yes** | 100% | | `caster_type`, else inferred from feature names |
| 🔴 | `cantrips_known_by_level`, `prepared_spells_by_level`, `spell_slots_by_level` | Spellcasting | | 0% | **`L`** | `ClassFeature.feature_type == CORE_TRAITS_TABLE` + `ClassFeatureItem.column_value` |
| 🔴 | **`features`** | Features | | 0% | **`L`** | `ClassFeatureItem.level` |

### 4.2 `subclass` — 101 entities

The worst row in the audit: **1 of 8** declared fields ever written.

| | Field | Group | Req | Fill | Cause | Source |
|:--:|---|---|:--:|--:|:--:|---|
| ✅ | `parent_class_ref` | Identity | **yes** | 100% | | `subclass_of`; 97 softRefs verified to resolve, 4 in-pack hard refs |
| 🔴 | **`granted_at_level`** | Identity | **yes** | 0% | **`L`** | `min(ClassFeatureItem.level)` for the subclass |
| 🔴 | `bonus_skill_pick_count` | Identity | | 0% | `M` | feature prose |
| 🔴 | `saving_throw_refs`, `weapon_proficiency_categories`, `armor_training_refs` | Progression | | 0% | `M` | feature prose |
| 🔴 | **`features`** | Features | | 0% | **`L`** | `ClassFeatureItem.level` |
| 🔴 | `flavor_description` | Features | | 0% | `M` | everything is folded into `description` |

### 4.3 `species` (11) / `subspecies` (30)

`mapSpecies` matches trait rows by **name** (`'size'`, `'speed'`, `'darkvision'`,
`'languages'`, `'ability score increase'`). Open5e ships a `type` field on every trait
(`MODIFICATION_TYPES`: `ability_score`, `skill_proficiency`, `tool_proficiency`,
`language`, `equipment`, `feature`, `feat`, …). Switching to `type` is the single fix
behind most of these rows.

| | Field | Group | Req | species | subspecies | Cause | Source |
|:--:|---|---|:--:|--:|--:|:--:|---|
| 🟡 | `size_ref` | Identity | **yes** (species) | 63% | 33% | `M` | `SpeciesTrait` "Size" — name match misses variants |
| ✅⚠ | `creature_type_ref` | Identity | **yes** | 100% | 100% | `M`/⚪ | hardcoded `Humanoid` on all 41 — defensible default, but not sourced |
| 🔴 | `age` | Identity | | 0% | 0% | `M` | "Age" trait row |
| 🟡 | `speed_ft` | Movement | **yes** (species) | 72% | 43% | `M` | "Speed" trait — name match |
| 🟡 | `speed_burrow/climb/fly/swim_ft` | Movement | | ≤9% | ≤3% | `M` | `_parseAltSpeeds` skips anything conditional |
| 🟡 | `granted_languages` | Grants | | 90% | 0% | `M` | trait `type == 'language'` |
| 🟡 | `ability_bonuses` | Grants | | 81% | 90% | `M` | trait `type == 'ability_score'`; "of your choice" unmodelled |
| 🟡 | `granted_senses` | Grants | | 63% | 0% | `M` | Darkvision only, **with no range**. Every shipped payload is a bare ref rather than a `{sense_ref, range_ft}` row — a shape the resolver explicitly accepts ("a sense with no stated range"), so it lands, at range 0. The fix is parsing the range out of the trait, not the shape. |
| 🟡 | `granted_skill_proficiencies` | Grants | | 45% | 26% | `M` | trait `type == 'skill_proficiency'` |
| 🟡 | `granted_damage_resistances` | Grants | | 36% | 13% | `M` | regex over prose |
| 🔴 | `granted_tool_proficiencies` | Grants | | 0% | 0% | `M` | trait `type == 'tool_proficiency'` never read |
| 🔴 | **`granted_feat_refs`** | Grants | | 0% | 0% | `M` | trait `type == 'feat'` never read |
| 🟡 | `granted_spell_refs` / `granted_cantrip_refs` | Grants | | 0% | 13%/10% | `M` | needs the "cast the X spell" phrasing |
| 🔴 | rest of the grant block (30 keys) | Grants | | 0% | 0% | `M`/⚪ | see §4.7 |
| 🔴 | `subspecies_options` | Lineage | | 0% | — | ⛔ | subspecies ship as their own entities |

### 4.4 `background` — 53 entities

| | Field | Group | Req | Fill | Cause | Source |
|:--:|---|---|:--:|--:|:--:|---|
| 🟡 | `granted_skill_refs` | Grants | **yes** | 96% | `M` | benefit `type == 'skill_proficiency'` |
| 🔴 | **`granted_tool_refs`** | Grants | | 0% | `M` | benefit `type == 'tool_proficiency'` — exists in `MODIFICATION_TYPES`, `descOfType` never asks for it |
| 🔴 | `granted_tool_variant_group` | Grants | | 0% | ⚪ | app-only |
| 🟡 | `granted_language_count` | Grants | | 58% | `M` | benefit `type == 'language'` |
| 🟡 | `ability_score_options` | Grants | **yes** | 50% | `S`/`M` | benefit `type == 'ability_score'`; pre-2024 docs have none |
| 🔴 | `asi_distribution_options` | Grants | **yes** | 0% | `S` | SRD-2024 concept; only emitted when 3 abilities are offered |
| 🔴 | `origin_feat_ref` | Grants | **yes** | 0% | `S` | only SRD-2024 backgrounds carry a `feat` benefit, and those docs are skipped |
| 🔴 | `starting_gold_gp`, `gold_alternative_gp` | Grants | | 0% | `M` | gold is parsed into the equipment group but never onto these fields |
| 🔴 | `default_inventory_refs` | Equipment | | 0% | ⛔ | superseded by `equipment_choice_groups` |
| 🟡 | `equipment_choice_groups` | Equipment | | 98% | | A/B parse, else fixed-kit fallback |

> Three **required** fields sit at 0–50%. Decide whether the requirement is wrong for
> third-party content or whether the SRD-overlap skip (§A2) needs revisiting.

### 4.5 `feat` — 73 entities

Prerequisites and ASI parse well. The grant block does not: **3 of 41 keys** are ever
written, so a feat's actual mechanical effect stays in prose.

| | Field | Group | Req | Fill | Cause | Notes |
|:--:|---|---|:--:|--:|:--:|---|
| ✅⚠ | `category_ref` | Identity | **yes** | 100% | `S` | every one of the 73 is `General` — the shipped docs predate the 2024 categories |
| 🟡 | `prerequisite` + `prereq_clauses` | Identity | | 37% / 30% | | parsed gates: ability/level/spellcasting/armor/weapon/skill |
| 🔴 | `prereq_class_refs`, `prereq_species_refs` | Identity | | 0% | `M` | named in prose |
| ✅⚠ | `repeatable` | Identity | **yes** | 100% | `M` | hardcoded `false` |
| 🔴 | `chooseable` | Identity | | 0% | ⚪ | defaults true |
| 🟡 | `asi_amount` / `_max_score` / `_ability_options` | Rules | | 31% | | two phrasings + "of your choice" |
| 🔴 | `bonus_skill_pick_count`, `bonus_expertise_pick_count`, `grants_save_prof_from_asi` | Rules | | 0% | `M` | |
| 🔴 | `benefits` | Rules | | 0% | ⛔ | folded into `description` |
| 🟡 | `granted_armor_proficiencies` | Grants | | 5% | `M` | |
| 🟡 | `speed_bonus_ft` | Grants | | 4% | `M` | |
| 🟡 | `player_choices` | Grants | | 2% | `M` | Skilled-style "choose N skills" only |
| 🔴 | the other 38 grant keys | Grants | | 0% | `M` | see §4.7 |

> `prereq_clauses` is written by the mapper and read by the UI but is **not a declared
> schema field** — it is invisible and uneditable in the card editor. Either declare it
> or stop writing it.

### 4.6 Non-chargen categories

**`spell` (1297)** — every required field green except `class_refs`.

| | Field | Fill | Cause | Source |
|:--:|---|--:|:--:|---|
| 🔴 | `class_refs` (**required**) | 0% | ⛔ | routed to `tags` on purpose — spell packs ship no class entities, so a `_ref` would dangle |
| 🔴 | `area_shape_ref`, `area_size_ft` | 0% | `M` | `Spell.shape_type` / `shape_size` / `shape_size_unit` are in the fixtures |
| 🔴 | `reaction_trigger` | 0% | `M` | `Spell.reaction_condition` |
| 🔴 | `at_higher_levels_text` | 0% | `L` | `SpellCastingOption.json`; today appended to `description` prose |
| 🔴 | `applied_condition_refs` | 0% | `M` | |
| 🔴 | `effects` | 0% | ⚪ | `spellEffectList` — a live named field with a real editor (**not** a removed DSL), but nothing in `domain/` or `application/` reads it, and Open5e has no structured damage rows. Leave empty until a reader exists. |
| 🟡 | `damage_type_refs` 23% · `save_ability_ref` 43% · `attack_type` 6% | | `S` | source-limited |

**`creature-action` (8615)** — one likely regression.

| | Field | Fill | Cause | Notes |
|:--:|---|--:|:--:|---|
| 🔴 | `damage_type_ref` | 0% | `M` | `open5e_import_roadmap.md` §4 says `damage_type` → `damage_type_ref` is mapped. It is not landing. **Confirm before filing.** |
| 🔴 | `save_dc`, `save_ability_ref`, `applied_condition_refs` | 0% | `M` | in the action `desc` |
| 🔴 | `recharge` | 0% | ⚪ | narrative twin of the typed `recharge_kind`, which is filled |
| 🔴 | `effects` | 0% | ⚪ | same `spellEffectList` field as above — no reader, no source |

**`monster` (2885)** — healthiest category.

| | Field | Fill | Cause | Notes |
|:--:|---|--:|:--:|---|
| 🟡 | `size_ref` | 99.9% | `S` | 2 titanic-size creatures, logged in `unmapped_report.json` |
| 🟡 | `alignment_ref` | 97% | `S` | 70 fuzzy 3rd-party alignments, logged |
| 🔴 | `tags_line` | 0% | `M` | the `type` subtype split never reaches the field |
| 🔴 | `lair_action_refs` | 0% | `M`/`S` | mapper emits it; no shipped creature has one |
| 🔴 | `gear_refs`, `spell_refs`, `cr_helper` | 0% | ⚪ | |

**`magic-item` (1063)**

| | Field | Fill | Cause | Notes |
|:--:|---|--:|:--:|---|
| ✅⚠ | `is_cursed`, `is_sentient`, `activation` | 100% | `M` | all three are hardcoded defaults |
| 🔴 | `cost_gp` | 0% | `S`/`M` | `MagicItem.cost` only written when > 0; confirm the source really is 0 |
| 🟡 | `weight_lb` | 10% | `S` | |
| 🔴 | `attunement_prereq` + `attunement_*` | 0% | `M` | `attunement_detail` is in the source |
| 🔴 | `charges_max`, `charge_regain`, `command_word`, `body_slot_ref`, `sentient_*` | 0% | `M` | in `desc` prose |
| 🔴 | `base_item_ref` | 0% | ⛔ | no base items ship in item packs |

**`trait` (6423)** — `trait_kind` is `Other` on all 6423 (⚠ hardcoded). The 43 grant-block keys are all 0%: a monster trait is display-only text.

**`adventuring-gear` (159)** — not real content. `_synthGearRef` invents these so background equipment refs resolve; `cost_cp` is `0` and `weight_lb` is `0` on all 159 ⚠, and **all 159 have an empty description**.

### 4.7 The shared grant block

`_FB.grantBlock` puts the same 41 keys — `CharacterResolver.grantFieldKeys`, the
closed contract — on `feat`, `species`, `subspecies`, `magic-item` and `trait`.
`applyGrantsFrom` is the single reader, so one shared helper in the importer
covers all five card types; do not fix this per category.

**`class`, `subclass` and `background` are not on that list, by design.** They use
their own typed fields, and anything level-gated goes on a `features` row pointing
at a Feat card (§0 rule 3). Adding the block to them fails `grant_contract_test`.

Across every bundled pack, exactly **seven** of the 41 keys are ever written:
`granted_skill_proficiencies`, `granted_languages`, `ability_bonuses`,
`granted_damage_resistances`, `granted_senses` (species/subspecies), plus
`granted_armor_proficiencies`, `speed_bonus_ft` and `player_choices` on a handful
of feats. The other 34 are 🔴 everywhere.

Highest value first:

1. **`mechanical_notes`** — 0% on all 6,423 traits and all 73 feats. Everything the
   §4 tables describe as "stays in prose" is meant to land here, one rule per
   line, and render under "Other Effects". Nothing else in this list makes a
   previously invisible rule visible.
2. `granted_senses` — needs `range_ft` parsed out of the trait text.
3. `granted_tool_proficiencies`, `granted_feat_refs` — both have a
   `MODIFICATION_TYPES` source tag that the mapper never reads.
4. `resource_pool_grants`, `extra_attack_count_by_level` — these are `{lvl: value}`
   tables, not formulas; they only become fillable after B1/B2 land the level data.

---

## 5. Roadmap

Ordered by leverage. Each phase has an exit criterion the audit tool can check.

- [ ] **A0 — Restore the source snapshot.**
      *Exit: `dart run tool/open5e_import/bin/build_packs.dart` reproduces the 19
      shipped packs byte-for-byte.*
- [ ] **A1 — Fill the fixture matrix (§3).** Every "never loaded" file gets a
      decision: load it, or record why not.
      *Exit: no `[ ] decide` rows left in §A1.*
- [ ] **B1 — Class/subclass level wiring.** Load `ClassFeatureItem.json`, group by
      `parent`, emit `features` rows (`level` + `name` + `description`) and
      `subclass.granted_at_level`.
      *Exit: `features` ≥ 95% on both categories, `granted_at_level` 100%, and
      `bundled_pack_resolve_test` shows a bundled subclass feature on the sheet.*
- [ ] **B2 — Class tables.** `feature_type == CORE_TRAITS_TABLE` + `column_value` →
      `cantrips_known_by_level` / `prepared_spells_by_level` / `spell_slots_by_level`
      / `extra_attack_count_by_level`.
- [ ] **B3 — Type-driven species/background mapping.** Match `SpeciesTrait.type` and
      `BackgroundBenefit.type` instead of names; add `tool_proficiency` and `feat`.
      *Exit: `size_ref`, `speed_ft`, `granted_skill_refs` at 100%.*
- [ ] **B4 — Spell gaps.** `shape_type`/`shape_size` → area fields;
      `reaction_condition` → `reaction_trigger`; load `SpellCastingOption.json` →
      `at_higher_levels_text`.
- [ ] **B5 — Grant block (§4.7).** Start with `mechanical_notes`: route every rule
      the mapper currently abandons in prose into it, one per line, so it reaches
      the sheet. Then the sourced keys. Also confirm/fix the
      `creature-action.damage_type_ref` regression and `monster.tags_line`.
- [ ] **B6 — Gear stubs.** Enrich the 159 from `Item.json`/`Weapon.json`/`Armor.json`,
      or drop them and reference built-in equipment instead.
- [ ] **B7 — Close the policy rows.** Every `P`/`N` row gets a one-line rationale so
      it stops reading as a bug. Decide what to do about the required-but-empty fields
      (`class.primary_ability_ref`, `background.origin_feat_ref`,
      `background.asi_distribution_options`, `spell.class_refs`): fill, or relax the
      requirement for third-party content.

### Done when

Every box above is ticked and every row in §4 is ✅, ⛔ or ⚪ — **no 🔴 or 🟡
without a cause code that says it cannot be fixed**. In app terms: installing a
third-party pack and picking one of its subclasses produces a character sheet with
that subclass's features on it. At that point this file becomes a record, and
`open5e_import_roadmap.md` goes back to being the primary doc.

---

## 6. Verification

```sh
cd flutter_app

# Regenerate every number in §2 and §4 (no source snapshot needed).
dart run tool/open5e_import/bin/audit_packs.dart
dart run tool/open5e_import/bin/audit_packs.dart --markdown   # doc-shaped tables
dart run tool/open5e_import/bin/audit_packs.dart --only class,subclass

# Needs the restored snapshot (A0):
dart run tool/open5e_import/bin/build_packs.dart --data ../open5e-api-staging/data
dart run tool/open5e_import/test/monster_mapper_check.dart

# Proves a mechanic reaches the sheet, not just the file.
flutter test test/domain/services/bundled_pack_resolve_test.dart
```

`bundled_pack_resolve_test.dart` is the right place to prove a B-phase landed: it
walks an asset the way an install does — resolve every `{_lookup, name}` against the
campaign's Tier-0 rows, then hand the entity to `CharacterResolver` — so it catches
the case where a field is written but nothing reads it.
