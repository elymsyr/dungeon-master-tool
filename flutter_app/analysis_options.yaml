# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Generated files — freezed/drift/riverpod/json_serializable output.
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/gen/**"
    - "lib/l10n/gen/**"
  errors:
    # Promote a few opinionated lints from info → warning so CI catches them.
    use_build_context_synchronously: warning
    cancel_subscriptions: warning
    close_sinks: warning
    # freezed forwards `@JsonKey(...)` from the factory constructor to the
    # generated field; the analyzer can't see that target and false-positives.
    # Standard freezed + json_serializable recommendation.
    invalid_annotation_target: ignore

linter:
  rules:
    # Safety-focused lints layered on top of flutter_lints defaults.
    - avoid_empty_else
    - cancel_subscriptions
    - close_sinks
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - use_build_context_synchronously
