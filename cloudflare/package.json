{
  "name": "dmt-assets-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker gatekeeper for DMT Cloudflare R2 asset bucket",
  "private": true,
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.81.1"
  }
}
