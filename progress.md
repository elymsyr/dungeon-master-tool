# Project Progress Report (progress.md)

## [14.06.2026 - 08:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 9: the **final** rule kind — `grant_pouch`).
- **Last Completed Task:** **PR-2.4 (slice 9) — the last rule kind lands: `grant_pouch` (data-driven class resources, §2.4 Barbarian-rage worked example). With this, ALL rule kinds are implemented.** A `when_granted` rule on a `recordList` field whose stored rows each declare a resource pouch; the fold reads each row, selects its max from a level→max table at the gate level, and emits a granted-pouch descriptor for the PC sheet. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — NEW value type **`GrantedPouch`** `{entityId, name, max:num, refillOn:String?}` with a **`pouchKey`** getter (`<entityId>:<name>` — the §2.4 `granted_pouches` persistence key) and a `toString`. `TemplateResolution` gains a **`Map<String,GrantedPouch> grantedPouches`** output + a reader **`grantedPouchFor(entityId,name)`**; `isEmpty` now also checks it. `resolve(...)` builds the map and threads it through `_foldRule` (signature extended) into the `switch (kind)` dispatcher, which gains the `grant_pouch` case (the `default:` is now reached **only** by a genuinely unrecognised kind — every kind in `RuleKinds` is handled). NEW **`_foldGrantPouch`** — gated by the folding triggers (the built-in declares it `when_granted` on the class `resources` field; the `gate` param `class`/`character` maps to the supplied `gateLevel`, the same discipline as `set_pouch_max`). Column names from params (defaults `name`/`max_by_level`/`refill_on`); rows read from the rule's own stored field (`attachment.values[field.fieldKey]`, `whereType<Map>`). Per row: name via `_stringFrom`; max via NEW **`_resolveGrantedMax`** (a number → fixed; a `"1:2,3:3,6:4"` string → NEW **`_parseLevelTableString`** → `{level:max}` then **`_selectAtLevel`** (reused from slice 7) at the gate; a Map → straight to `_selectAtLevel`; a non-scalar selection → deferral); refillOn via `_stringFrom`. A row whose progression has **no entry ≤ the gate** is **not granted yet** (locked resource — not a skip, mirroring slice-7 empty contributions); an unnamed row is skipped; no stored rows / a garbled table is **deferred** (never silently dropped). Library/`RuleKinds`/`TemplateResolution` doc comments updated — they now read **"all rule kinds implemented"**; the deferred list describes only *unrecognised* kinds.
  - **`tool/template_rule_resolver_harness.dart` (edited)** — NEW **Barbarian** attachment (`entityId 'class-barbarian'`, a `recordList` `resources` field) with the §2.4 `grant_pouch` rule and two rows: **Rage** `"1:2,3:3,6:4,12:5,17:6"` refill `long_rest` and **Brutal Strike** `"9:1,13:2"` refill `short_rest`. Added to the `resolve(...)` set; NEW `Granted pouches` print block + **`Grant-pouch self-check`** (`grantedPouches.length==1`; Rage `max==3` — the L3 step is the highest ≤ gateLevel 5, i.e. a L5 barbarian's 3 rages — `refillOn=='long_rest'`, `pouchKey=='class-barbarian:Rage'`; **Brutal Strike `null`** — its L9 progression is locked at L5). `Overall` now ANDs it. The Deferred block's expectation is now "0 (ALL rule kinds now resolve)". Every prior block (stat overlay, notes, warnings, grants/proficiency, pending choices, pouch maxima, button runtime, `deferred==0`) is unchanged.
  - **Correction caught in this slice:** the prior "Next Task" note loosely said Rage → "2 at L5". The faithful step-table semantics (and real D&D: L1–2 → 2, **L3–5 → 3**, L6+ → 4) give **3** at L5, which is what `_selectAtLevel` returns. The harness asserts the correct value (`max==3`); a Python port of `_parseLevelTableString` + `_selectAtLevel` confirmed Rage→3, Brutal Strike→locked.
  - **Scope / authority discipline:** SHADOW ONLY — `grant_pouch` is interpreted only inside `resolve(...)`; the single `TemplateResolution(...)` construction site is in `resolve` and `resolve(`/`applyButton(` are called only by the harness (the live sheet still uses `CharacterResolver`). No `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — `grant_pouch` reuses the slice-1 trigger gating, slice-5 `_stringFrom`, and the slice-7 `_selectAtLevel`; only the `GrantedPouch` type + the `grantedPouches` output + the table-string parser were added.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: comment/string-stripped bracket balance exact on both files (resolver `{}`183/183 `()`486/486 `[]`121/121; harness `{}`126/126 `()`198/198 `[]`79/79); the slice-9 maths (parse → `_selectAtLevel` at gate 5) cross-checked with a Python port → Rage `max==3`, Brutal Strike `null`. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.5 — the `choose` *resolution* re-fold (apply a recorded selection's `perPick` effects).** All rule kinds now *emit/fold*; the one piece deferred within a kind is applying a made `choose` selection: read the PC card's `rule_choices[<choiceKey>] = [<picked>,…]` (§4.4 step 4), and for each picked option run the rule's nested `perPick` effects (e.g. the Athlete ASI's `modify_stat target:'ability:{row.ability}' value:'{row.amount}'`) through the same fold — with `{row.*}`/`{pick}` interpolation against the chosen row. Thread a `Map<String,List<String>> ruleChoices` (or per-attachment selections) into `resolve(...)`; a choice WITH a recorded selection folds its `perPick` and no longer surfaces a `PendingChoice`. Add a harness asserting the Athlete, given `['str']`, yields `str +1` and emits no pending choice. After that, PR-2.6+ is the Phase-3 call-site flip / debug panel (old-vs-new comparison) and the rule-editor UI (PR-3.5a). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…9 harness runs + `flutter analyze`.)*

## [14.06.2026 - 07:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 8: the imperative pouch pair — `refill_pouch` / `empty_pouch`).
- **Last Completed Task:** **PR-2.4 (slice 8) — the imperative `on_button` pouch pair lands: `refill_pouch` / `empty_pouch` (§4.2).** With the seven *folding* kinds done, this slice adds the two kinds that are **not** folds — they mutate stored pouch *current* values in response to a rest / level-up button press (§2.3: refill/empty rules are declared **on the target pouch field**; §6 item 6: buttons are imperative, not folds). Because they mutate state rather than derive an overlay, they get a **separate resolver entry point** — `applyButton(...)` — rather than going through `resolve(...)`. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — NEW public **`applyButton(attachments, {required button, pouchMax, currentValues, aspects})` → `Map<String,dynamic>`** (each touched pouch field key → its NEW current: a scalar `num` for an `intPouch`, a per-row `Map<String,num>` for a `pouchMatrix`). It walks the attachments' fields (same deterministic order as the fold — attachment order → `orderIndex` → rule index), firing a rule when it is a `refill_pouch`/`empty_pouch` kind, its `trigger == on_button`, and its **`params.button` (or top-level `button`) equals `button`**. The acted-on pouch is `params.target`/top-level `target` **defaulting to the field's own key** (refill/empty are declared on the pouch field, so the common case needs no `target`). Multiple rules on the same pouch **chain in fold order** (each sees the prior result). NEW helpers: **`_applyPouchButton`** (dispatches per-row for a matrix when `max`/`prior` is a `Map`, else scalar; returns `null` to leave a pouch untouched rather than corrupt it); **`_computePouchCurrent`** (the scalar maths — refill `all`/absent → `max` (full), else `min(prior + amount, max)`; empty `all`/absent → `0`, else `max(prior − amount, 0)`); **`_pouchAmountRaw`** (top-level then `params`); **`_pouchAmountValue`** (`half_max_round_up` → `ceil(max/2)`; a number → itself; a string → a §4.3 `FormulaEvaluator` formula over the `AspectContext`, a bad/erroring formula → `null` so the pouch is left as-is). The `_foldRule` `switch` gains stacked **`case refill_pouch: case empty_pouch: break;`** — during a *fold* these are correctly **inert** (a fold is not the button runtime; NOT a silent drop — they are handled by `applyButton`). Library/`RuleKinds` doc comments updated; the deferred-kinds list narrowed to the **single remaining** kind, `grant_pouch`.
  - **`tool/template_rule_resolver_harness.dart` (edited)** — NEW **`pcSheet`** attachment with four button-bearing pouch fields, exercising all four amount paths and reusing the SAME run's slice-7 `set_pouch_max` overlay as the max source: `spell_slots` (pouchMatrix, refill `long_rest` `all` → per-row current restored to max `{1:5,2:4,3:2}`), `ki_points` (intPouch, refill **SHORT** rest `half_max_round_up` → `ceil(5/2)=3` from prior 0), `sorcery_points` (intPouch, refill `long_rest` formula `'level'` → `min(0+5,5)=5`, exercising a formula amount AND the max cap), `rage` (intPouch, empty `long_rest` `all` → 0 from prior 2). NEW `applyButton` calls for `long_rest` and `short_rest` + a `Button runtime` print block + **`Button self-check`** (`long_rest` → `{spell_slots:{1:5,2:4,3:2}, sorcery_points:5, rage:0}` length 3, **no** `ki_points`; `short_rest` → `{ki_points:3}` length 1, **no** `spell_slots`); `Overall` now ANDs it. Every prior block (stat overlay, notes, warnings, grants/proficiency, pending choices, pouch maxima, `deferred==0`) is unchanged — `pcSheet` is used ONLY by `applyButton`, never added to the `resolve(...)` set, so the fold output is byte-identical to slice 7.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve` or `applyButton` (both are only called from the harness; the live sheet still uses `CharacterResolver`); no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — `applyButton` reuses the slice-1 trigger constants, the slice-7 `pouchMax` overlay shape, the slice-3 `FormulaEvaluator`, and `_ruleParamString`/`_coerceNum`; only the button entry point + the four pouch helpers were added.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: comment/string-stripped bracket balance exact on both files (resolver `{}`164/164 `()`428/428 `[]`109/109; harness `{}`118/118 `()`185/185 `[]`76/76); the slice-8 button maths (fire-match → per-row/scalar → `min(prior+amt,max)` / `max(prior−amt,0)`, with `all`/`half_max_round_up`/formula amounts) was **cross-checked with a Python port** → `long_rest == {spell_slots:{1:5,2:4,3:2}, sorcery_points:5, rage:0}` (3 keys, no ki), `short_rest == {ki_points:3}` (1 key, no slots), matching the harness self-check. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 9) — the final pouch kind: `grant_pouch` (data-driven class resources).** With `refill_pouch`/`empty_pouch` done, the LAST rule kind remains (§4.2 / the §2.4 Barbarian-rage worked example): a `when_granted` rule on a `recordList` field whose rows each define a resource pouch — `nameCol` (e.g. "Rage"), `maxTableCol` (a `"1:2,3:3,6:4,…"` level→max string, selected at the gate level), `refillCol` (`short_rest`/`long_rest`/`level_up`), gated by `gate` (`class`/`character`). It should emit, per row, a **granted pouch descriptor** (a NEW `TemplateResolution` output — e.g. `Map<String, _GrantedPouch>` keyed by `<entityId>:<name>`, carrying `{name, max, refillOn}`) so the PC sheet can render a "Rage 2/2" intPouch and persist current under `granted_pouches`. Reuse `_selectAtLevel` for the max-table read. Add a harness Barbarian asserting a Rage pouch with max 2 at gate 5 (`1:2,3:3,6:4,…` → 2 at L5) refilling on `long_rest`. **After slice 9, ALL rule kinds are implemented** — the slice cycle then moves to PR-2.5+ (the choice *resolution* re-fold, then the Phase-3 call-site flip / debug panel). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…8 harness runs + `flutter analyze`.)*

## [14.06.2026 - 06:13] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 7: the pouch family begins — `set_pouch_max`).
- **Last Completed Task:** **PR-2.4 (slice 7) — the `set_pouch_max` rule kind lands: it sets a pouch *maximum* (→ `TemplateResolution.pouchMax`) from a level-keyed progression or a formula, aggregating across multiclass (§4.2).** With the scalar (`modify_stat`), text (`note`/`check_clauses`), collection (`grant_refs`/`grant_proficiency`) and choice (`choose`) kinds done, this slice opens the last group — the pouch kinds — with the one that *derives* a max. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — `TemplateResolution` gains a **`Map<String,dynamic> pouchMax`** overlay (value = a `num` for an `intPouch` scalar max, or a `Map<String,num>` for a `pouchMatrix` per-row max), plus two readers — **`pouchMaxFor(fieldKey)`** (→ the scalar/map or `null`) and **`pouchRowMax(fieldKey,rowKey)`** (→ a row's `num?`); `isEmpty` now also checks it. `resolve(...)` builds the map and threads it through `_foldRule` (signature extended) into the `switch (kind)` dispatcher, which gains the `set_pouch_max` case. **`_foldSetPouchMax`** — gated by the **folding triggers** (reuses `_triggerActive`/`_isFoldingTrigger`; the built-in declares it `when_granted` on a class field — the **gate level**, not the trigger, selects the progression row). NEW helpers: **`_resolvePouchSource`** (source = `rule['source']`/`params.source` → `{kind:'formula',expr}` evaluated over the `AspectContext` for a scalar, or `{kind:'field',field}` reading a stored level-keyed map; **defaults to the rule's own stored field** — the slot-progression `levelMatrix`/`levelTable`); **`_selectAtLevel`** (picks the entry whose int key is the highest ≤ gate — the §4.3 `table(...)` step; D&D slot tables are cumulative so the single row is the full count; a Map value → per-row `Map<String,num>`, a num value → scalar; **no row ≤ gate ⇒ a valid-but-empty contribution, not a skip**); **`_accumulatePouchMax`** (sums a contribution into the target — per-row for a matrix — so multiclass casters AGGREGATE; a scalar-vs-matrix **shape clash** on the same target is surfaced as a deferral, never silently dropped). A non-folding trigger / missing `target` / unresolvable source also defer. Class/`RuleKinds`/`TemplateResolution` doc comments updated; the deferred-kinds list narrowed to the **two remaining** pouch kinds (`refill_pouch`/`empty_pouch`, `grant_pouch`).
  - **`tool/template_rule_resolver_harness.dart` (edited)** — five NEW attachments exercising every `set_pouch_max` path: **Wizard** (`levelMatrix`, own-field source → per-row max on PC `spell_slots`, level-5 row `{1:4,2:3,3:2}`); **Cleric** (multiclass, explicit `{kind:'field'}` source → SAME `spell_slots` target, row `{1:1,2:1}` → the per-row **aggregation** sums to `{1:5,2:4,3:2}`, tier 3 from the wizard alone); **Ki Monk** (`levelTable` → scalar `ki_points`=5); **Sorcerer** (`{kind:'formula',expr:'level'}` → scalar `sorcery_points`=5); **Paladin** (slots begin at L7 → **gated out** at gateLevel 5, `pouchMaxFor('paladin_slots')==null`, NOT a deferral). NEW `Pouch maxima` print block + **`Pouch self-check`** (`pouchMax.length==3`; `spell_slots` row maxima 5/4/2; `ki_points==5`; `sorcery_points==5`; `paladin_slots==null`); `Overall` now ANDs it. The stat overlay, notes (1), warnings (1), grants/proficiency, pending choices (2) and **`deferred==0`** are all unchanged.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve` (`TemplateResolution(` is constructed only at resolver:368 + the `const` ctor at 251; `resolve(` only in the harness — verified by grep; the other `*.resolve` hits are the old `CharacterResolver`/asset resolver); no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — `set_pouch_max` reuses the slice-1 trigger gating, the slice-2 `field` source pattern and the slice-3 `FormulaEvaluator`; only the `pouchMax` output + the three pouch helpers were added.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: bracket balance exact on both files (resolver `{}`139/139 `()`365/365 `[]`95/95; harness `{}`108/108 `()`171/171 `[]`62/62, comment/string-stripped); the slice-7 fold (source resolve → `_selectAtLevel` at gate 5 → `_accumulatePouchMax`) was **cross-checked with a Python port** → `spell_slots=={1:5,2:4,3:2}` (wizard+cleric per-row sum), `ki_points==5`, `sorcery_points==5`, `paladin_slots` correctly absent (gated out), `pouchMax.length==3` — matching the harness self-check. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 8) — the imperative pouch pair: `refill_pouch` / `empty_pouch`.** With `set_pouch_max` done, add the `on_button` imperative kinds (§4.2) declared **on the target pouch field**: a `button` (`long_rest`/`short_rest`/`level_up`) and an `amount` (`all` / `half_max_round_up` / a formula). Unlike the folding kinds these are *event-driven* — a button press walks the PC's pouch fields and fires matching rules, setting `current` from `max` (refill) or to 0 (empty). Model them as a separate resolver entry point (e.g. `applyButton(attachments, button, currentValues)` → updated `{current}` per pouch) since they mutate stored state rather than fold a derived overlay; add a harness asserting a `long_rest` refill restores `current=max` and `half_max_round_up` computes `ceil(max/2)`. Then the final kind `grant_pouch` (data-driven Rage/Ki/Channel-Divinity resource pouches from a recordList). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…7 harness runs + `flutter analyze`.)*

## [14.06.2026 - 05:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 6: the `choose` kind — pending-choice emission).
- **Last Completed Task:** **PR-2.4 (slice 6) — the `choose` rule kind lands: a rule that *emits a pending choice* (→ `TemplateResolution.pendingChoices`) rather than folding a final value (§4.2 / §4.4 step 4).** With the scalar (`modify_stat`), text (`note`/`check_clauses`) and collection (`grant_refs`/`grant_proficiency`) kinds done, this slice adds the kind that surfaces an *unanswered decision* — ASI picks, subspecies, equipment groups, expertise, fighting style. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — NEW **`PendingChoice`** value type `{entityId, fieldKey, ruleId, prompt, options:List<String>, pick:int, target?}` with a **`choiceKey`** getter (`<entityId>:<fieldKey>:<ruleId>` — the §4.4-step-4 `rule_choices` persistence key) and a `toString`. `TemplateResolution` gains a **`List<PendingChoice> pendingChoices`** output and a **`choiceFor(entityId, fieldKey)`** reader (→ the surfaced choice or `null`); `isEmpty` now also checks it. `resolve(...)` builds the list and threads it through `_foldRule` (signature extended) into the `switch (kind)` dispatcher, which gains the `choose` case. **`_foldChoose`** — gated by the **folding triggers** (reuses `_triggerActive`/`_isFoldingTrigger`, exactly like the grant kinds: `when_granted` always, `when_equipped` only when equipped, `level_up` only ≥ gate; a non-folding trigger / no resolvable options is **deferred**, never silently dropped). Options are gathered via the existing **`_gatherStrings`** (inline `rule['options']` → `params['options']` → the rule's own stored field value `attachment.values[field.fieldKey]` — so `optionsFrom:'rows'`/`'refs'` both resolve from the field; mapKeys broadened to `id/ref/value/ability/name/option/entity_id/slug/choiceId`). NEW helpers **`_choosePick`** (`pick` top-level or under `params`, clamped ≥ 1, default 1) and **`_ruleParamString`** (read a string param top-level then under `params` — used for `prompt`/`target`/`ruleId`; `prompt` falls back to a `pick`-aware default; `ruleId` falls back to `rule#<index>`). The `perPick` nested effects are intentionally **not** applied this slice (re-folding a made selection is a following slice). Class/`RuleKinds`/`TemplateResolution` doc comments updated; the deferred-kinds list narrowed to the three remaining pouch kinds (`set_pouch_max`, `refill_pouch`/`empty_pouch`, `grant_pouch`).
  - **`tool/template_rule_resolver_harness.dart` (edited)** — two NEW attachments. **Athlete** feat exercises `choose` with `optionsFrom:'rows'` (params-nested): options read from its OWN stored recordList (`asi_options` rows → `['str','dex']` via the `ability` col), `pick:1`, a `prompt`/`target`, and a carried-but-unapplied `perPick` modify_stat. **Fighter** exercises `choose` with **INLINE** options (top-level path) and the **`level_up`** gate: the fighting-style choice (gated L1) surfaces at gateLevel 5, while a second style (gated **L11**) must **NOT** surface (gated out → no choice, no skip). NEW `Pending choices` print block + **`Choose self-check`** (`pendingChoices.length==2`; the ASI choice's exact options/pick/prompt/target/`choiceKey=='feat-athlete:asi_options:rule#0'`; the style choice's 4 inline options/pick/prompt/target; and `choiceFor('class-fighter','second_style')==null`); `Overall` now ANDs it. The stat overlay (`ac +4, speed +10, str +4, max_hp +10, unarmored_ac +11, prof_check +3`), notes (1), warnings (1), grants/proficiency, and **`deferred==0`** are all unchanged — the choose rules emit/gate, they don't fold or defer.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve` (`TemplateResolution(` is constructed only at resolver:330; `resolve(` only in the harness — verified by grep; the other `*.resolve` hits are the old `CharacterResolver`/asset resolver); no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — `choose` reuses the slice-1 trigger gating and the slice-5 `_gatherStrings`; only the `PendingChoice` type + the `pendingChoices` output were added.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: bracket balance exact on both files (resolver `{}`107/107 `()`282/282 `[]`74/74; harness `{}`81/81 `()`141/141 `[]`52/52, comment/string-stripped); the slice-6 fold (gather options → pick/prompt/target → emit, with the `level_up` gate) was **cross-checked with a Python port** → athlete `options==['str','dex']`/`choiceKey=='feat-athlete:asi_options:rule#0'`, fighter `options==['Archery','Defense','Dueling','Great Weapon Fighting']`, and the L11 `second_style` correctly **does not surface** (`pendingChoices.length==2`), matching the harness self-check. Dart-specifics confirmed: the `switch (kind)` gains one more `const String` case (no `break`, `default:` stays exhaustive-clean); `PendingChoice` is a `const` value type; `pendingChoices` literal `<PendingChoice>[]` is type-correct; `choiceFor` returns `PendingChoice?`. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 7) — the pouch family begins: `set_pouch_max`.** With scalar/text/collection/choice output kinds done, start the last group — the pouch kinds (§4.2). Begin with **`set_pouch_max`**: target a `intPouch`/`pouchMatrix` fieldKey and set its max from a source (a `levelTable`/`levelMatrix` field read, or a `formula` over the `AspectContext`), **aggregating across multiclass** (sum maxima from multiple class attachments). Extend `TemplateResolution` with a `Map<String,dynamic> pouchMax` overlay (scalar for intPouch, per-row map for pouchMatrix), emit it gated by the folding triggers, and add a harness attachment (e.g. a class slot-progression field setting spell-slot maxima) asserting the aggregated max. Then the remaining pouch kinds: `refill_pouch`/`empty_pouch` (the `on_button` imperative pair) and `grant_pouch` (data-driven resource pouches — Rage/Ki/Channel Divinity). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…6 harness runs + `flutter analyze`.)*

## [14.06.2026 - 04:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 5: the collection-output kinds — `grant_refs` + `grant_proficiency`).
- **Last Completed Task:** **PR-2.4 (slice 5) — the two collection-output rule kinds land: `grant_refs` (→ `TemplateResolution.grants`) and `grant_proficiency` (→ `TemplateResolution.proficiencyGrants`).** With the scalar (`modify_stat`) and text (`note`/`check_clauses`) kinds done, this slice adds the kinds that produce *list/map* outputs rather than a number or a string. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — `TemplateResolution` gains **two output collections**: `Map<String,List<String>> grants` (`grant_refs`: target list-field key → deduped ref ids, e.g. `{'resistances': ['fire','cold','poison']}`) and `Map<String,Map<String,String>> proficiencyGrants` (`grant_proficiency`: skillTree fieldKey → `{rowName: tier}`), plus two readers — **`grantsFor(fieldKey)`** (→ refs, `const []` when none) and **`proficiencyFor(fieldKey,row)`** (→ tier or `null`); `isEmpty` now also checks the two maps. `resolve(...)` builds both maps and threads them through `_foldRule` (signature extended) into the `switch (kind)` dispatcher, which gains the `grant_refs`/`grant_proficiency` cases. **`_foldGrantRefs`** — gated by the **folding triggers** (reuses `_triggerActive`/`_isFoldingTrigger`, exactly like `_foldModifyStat`: `when_granted` always, `when_equipped` only when equipped, `level_up` only ≥ gate; a non-folding trigger / missing `target` / no resolvable refs is **deferred**, never silently dropped). Refs are gathered via the new **`_gatherStrings`** (inline `rule['refs']` → `rule['params']['refs']` → the rule's own stored field value `attachment.values[field.fieldKey]`; a bare scalar tolerated as a singleton list), each coerced by **`_stringFrom`** (bare `String`, `num`, or a `Map` carrying the id under `id`/`ref`/`entity_id`/`slug`); accumulated into `grants[target]` **de-duplicated in fold order** (`bucket.contains` guard). **`_foldGrantProficiency`** — same trigger gate; `target` = skillTree fieldKey, `tier` ∈ `proficient`(default)/`expertise` via **`_proficiencyTier`** (top-level or under `params`), rows via the same `_gatherStrings` (inline `rule['rows']` / `params.rows` / stored field; map keys `name`/`row`/`skill`/`ability`); recorded into `proficiencyGrants[target]` with **`_higherTier`** so `expertise` **never gets downgraded** by a later `proficient` grant on the same row. Class/`RuleKinds`/`TemplateResolution` doc comments updated; the deferred-kinds list narrowed to the four remaining (`choose`, `set_pouch_max`, `refill_pouch`/`empty_pouch`, `grant_pouch`).
  - **`tool/template_rule_resolver_harness.dart` (edited)** — the existing **Tough feat** `grant_refs` stub becomes a *resolving* case: it now carries `values:{grant_resistance:['fire','cold']}`, so its `grant_refs` reads its own stored field into `resistances` (**`deferred` drops 1 → 0**). NEW **Half-Elf** species exercises `grant_refs` with **INLINE map-refs** (`[{id:'fire'},{id:'poison'}]`) and **de-duplication** (the repeated `fire` is dropped → `resistances == ['fire','cold','poison']` in fold order). NEW **Rogue** exercises `grant_proficiency`: `expert_skills` (field-rows recordList → `expertise` on Stealth + Sleight of Hand) then `base_skills` (INLINE rows → `proficient` on Stealth + Perception) — asserting the **tier-precedence** rule (Stealth stays `expertise`, not downgraded) and the inline-vs-field row paths. NEW Grants/Proficiency print blocks + **`Grants self-check`** (resistances exact order/length, `grants.length==1`) and **`Proficiency self-check`** (`skills` exactly `{Stealth:expertise, Sleight of Hand:expertise, Perception:proficient}`, both map sizes); `Overall` ANDs the two. The stat overlay (`ac +4, speed +10, str +4, max_hp +10, unarmored_ac +11, prof_check +3`), notes (1), and warnings (1) are unchanged.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve` (`TemplateResolution(` is constructed only inside the resolver — verified by grep); no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — both kinds reuse the slice-1 trigger gating and the attachment values; only the two output collections were added to `TemplateResolution`.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: bracket balance exact on both files (resolver `{}`93/93 `()`246/246 `[]`66/66; harness `{}`68/68 `()`125/125 `[]`41/41, comment/string-stripped); the slice-5 fold (gather → coerce → dedup / tier-precedence) was **cross-checked with a Python port** → `resistances == ['fire','cold','poison']` and `skills == {Stealth:expertise, Sleight of Hand:expertise, Perception:proficient}`, both matching the harness self-checks. Dart-specifics confirmed: the `switch (kind)` gains two more `const String` cases (no `break` needed, `default:` keeps it exhaustive-clean); `putIfAbsent(target, () => <String>[])`/`<String,String>{}` return the typed bucket; `Map<String,List<String>>`/`Map<String,Map<String,String>>` literals + the `grantsFor`/`proficiencyFor` readers are type-correct; the `.length == 0` → `result.deferred.isEmpty` swap avoids the `prefer_is_empty` lint. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 6) — the `choose` kind (pending-choice emission).** With the scalar/text/collection output kinds done, add `choose` (§4.2 / §4.4): a rule that emits a *pending choice* rather than folding a final value — `optionsFrom` (rows/refs or an inline option list), `pick` N, a `prompt`, an optional `target`, and (later) `perPick` nested effects (ASI picks, subspecies, expertise, fighting style). Extend `TemplateResolution` with a `List<PendingChoice>` (`{entityId, fieldKey, prompt, options, pick, target}`), emit one per active `choose` rule (gated by the folding triggers), and add a harness attachment asserting the surfaced choice. Defer the *resolution* of a made choice (re-folding `perPick` effects from a stored selection) to a following slice. After `choose`, the **pouch family** remains: `set_pouch_max`, `refill_pouch`/`empty_pouch`, `grant_pouch`. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…5 harness runs + `flutter analyze`.)*

## [14.06.2026 - 03:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 4: the first non-`modify_stat` kinds — `note` + `check_clauses`).
- **Last Completed Task:** **PR-2.4 (slice 4) — the two text-producing rule kinds land: `note` (→ `TemplateResolution.notes`) and `check_clauses` (→ `TemplateResolution.warnings`).** Slices 1–3 completed `modify_stat` (all three value sources); this slice begins the remaining rule kinds with the two that need no new infrastructure — they read the already-built `AspectContext`/attachment values and write the previously-empty `notes`/`warnings` overlay lists. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). Two files edited:
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — `_foldRule` was refactored from an `if (kind != modify_stat) defer` guard into a **`switch (kind)` dispatcher**; the `modify_stat` body moved verbatim into a new `_foldModifyStat(...)` (behavior byte-identical — same trigger gate, target, value-source resolution), and an empty `kind` now defers with `'rule missing "kind"'`. `resolve(...)` threads its `notes`/`warnings` lists into `_foldRule`. **`_foldNote`** — emits `rule['text']` (or `rule['note']`) into `notes` when active; `{field_key}` placeholders are interpolated from `attachment.values[...]` via `_interpolate` (`RegExp(r'\{([A-Za-z0-9_]+)\}')`; unknown keys left verbatim); a blank text → a `'note rule missing "text"'` deferral (never a silent drop). Gating via **`_noteActive`**: a note always surfaces (it is informational) EXCEPT `when_equipped` (only equipped) and `level_up` (only once `gateLevel >= at_level`). **`_foldCheckClauses`** — gathers clauses via **`_gatherClauses`** (inline `rule['clauses']` → `rule['params']['clauses']` → fallback to the field's own stored rows `attachment.values[field.fieldKey]`, the production `prereq-clauses` recordList shape) and evaluates each `{aspect|field, op, value}` via **`_evalClause`** against the `AspectContext` (ops `>= <= > < == = !=`; value coerced via the existing `_coerceNum`). An **unmet** clause pushes a policy-tagged warning `'<entityId> [<policy>]: requires <aspect> <op> <value> (have <actual>)'` (`policy` ∈ `block`/`warn`, default `warn`, read top-level or under `params` via `_clausePolicy`); a **malformed** clause (missing aspect/value, unknown op) pushes a distinct `'<entityId> [clause error]: <reason>'` so a data bug is never read as "passing"; **no clauses ⇒ trivially satisfied** (not a skip). Gating via **`_checkActive`**: checked whenever the entity is present, except the equip-scoped triggers (`when_equipped`/`prereq_to_equip`), which require `isEquipped`. New shared **`_skip(...)`** helper builds the `ResolverSkip` (replaces the per-call local closure now that three kinds share it). Class/`RuleKinds`/`TemplateResolution` doc comments updated to record the two new implemented kinds; the deferred-kinds list narrowed to the six collection/pouch kinds.
  - **`tool/template_rule_resolver_harness.dart` (edited)** — two NEW attachments: a **Flame Tongue** sword (`note` under `when_equipped`, `text` with a `{fire_bonus}` placeholder → interpolated to `"…extra 2 fire damage…"`), and a **Grappler**-style feat (`check_clauses` under `prereq_to_grant`, `policy:block`, clauses stored as the field's **recordList rows** — exercising the field-rows fallback, NOT inline: `str 16>=13` passes, `con 9>=13` FAILS → one `[block]` warning). Both added to the `resolve(...)` set; NEW `Notes`/`Warnings` print blocks + **`Note self-check`** (notes==1 and exact text) and **`Warning self-check`** (warnings==1, contains `feat-grappler`/`[block]`/`con`/`(have 9)`); `Overall` now ANDs the two new checks. The stat overlay (`ac +4, speed +10, str +4, max_hp +10, unarmored_ac +11, prof_check +3`) and `deferred==1` (only `grant_refs`) are unchanged — the new kinds resolve, they don't defer.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve`; no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. No new infrastructure — both kinds reuse the slice-2 `AspectContext` and the attachment values.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: bracket balance exact on both files (resolver `{}`66/66 `()`169/169 `[]`43/43; harness `{}`53/53 `()`98/98 `[]`28/28, comment/string-stripped); the slice-4 semantics (note interpolation + clause evaluation/warning formatting) were **cross-checked with a Python port** — note → `"While ablaze, deals an extra 2 fire damage on a hit."` and the grappler's clauses → exactly one `"feat-grappler [block]: requires con >= 13 (have 9)"`, both matching the harness self-checks. Dart-specifics confirmed: the Dart-3 statement `switch (kind)` over `const String` cases needs no `break` and each case implicitly terminates; the `switch (op)` **expression** with the `'==' || '=' =>` logical-or pattern and a `_ => null` arm infers `bool?`; `whereType<Map>().toList()` yields `List<Map<dynamic,dynamic>>` (matches `_evalClause`'s param + the return type); `FieldType.recordList` exists (field_schema.dart:60). The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 5) — the collection-output kinds: `grant_refs` + `grant_proficiency`.** With the text kinds done, add the two kinds that produce list/collection outputs rather than a scalar overlay. **`grant_refs`** (§4.2: traits/languages/resistances/spells/actions) — read the rule's `target` PC list-field key and the refs from the field value (`attachment.values[field.fieldKey]` — a list of entity-id refs), and accumulate them into a new `TemplateResolution.grants` map (`{targetFieldKey: [refs…]}`), de-duplicated, gated by the folding triggers. **`grant_proficiency`** (§4.2) — target a `skillTree` fieldKey + tier (`proficient`/`expertise`) + the rows from field refs, emitting a `TemplateResolution.proficiencyGrants` overlay (`{fieldKey: {rowName: tier}}`). Extend `TemplateResolution` with the two new output maps, add a `grant_refs` attachment to the harness asserting the collected refs (the existing Tough-feat `grant_refs` stub becomes a *resolving* case → `deferred` drops 1 → 0), and a `grant_proficiency` attachment. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1…4 harness runs + `flutter analyze`.)*

## [14.06.2026 - 02:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 3: the `formula` value source + `formula_evaluator.dart`)
- **Last Completed Task:** **PR-2.4 (slice 3) — the `formula` value source is live, backed by a new `formula_evaluator.dart` implementing the §4.3 grammar.** Slices 1–2 stood up the resolver with the `fixed` and `field` value sources and the `AspectContext`; this slice closes out the third (last) `modify_stat` value source, so `modify_stat` is now fully implemented. Still SHADOW-ONLY (no call-site flip; `character_resolver.dart` remains authoritative until Phase 3.11). One NEW file + two edited:
  - **`domain/services/template_rules/formula_evaluator.dart` (NEW, ~340 lines incl. docs)** — a self-contained recursive-descent lexer+parser+evaluator for the the-template-system §4.3 formula grammar, evaluated against an [AspectContext]. Public surface: **`FormulaEvaluator`** (`const`, `num evaluate(String expr, AspectContext aspects)`) and **`FormulaException`** (thrown on any lex/parse/eval error; the resolver catches it). Grammar: **identifiers = aspect names** (`aspects.value(name)`, **0 when absent** per §4.3, so an unpublished aspect contributes 0 rather than throwing); **integer/decimal literals**; binary **`+ - * /`** with standard precedence (`*`/`/` bind tighter than `+`/`-`) and **unary `-`/`+`**; functions **`floor(x)` `ceil(x)` `min(a,b,…)` `max(a,b,…)`** (min/max variadic, ≥1 arg); **`table(expr, "1:2,5:3,9:4,…")`** — a step table returning the value of the highest `threshold <= expr` (0 below all; malformed pairs skipped, never thrown); and **parentheses**. **No conditionals** (§4.3 — predicates become a `note`). `/` is **real division** (the DM wraps in `floor()` for D&D maths; `/0` throws `FormulaException`). The lexer special-cases the computed **`class_level(<slug>)`** aspect — it captures `class_level(...)` as one whole identifier token by scanning to the matching `)`, so a **hyphenated slug** (e.g. `battle-master`) is NOT mis-lexed as `battle − master`; that whole string is then an aspect lookup. String literals (the `table` spec) accept single or double quotes. The parser is one `_Parser` instance per `evaluate` (expression → term → factor → primary), with `expectEnd()` rejecting trailing tokens.
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — imports `formula_evaluator.dart`; the `_resolveValueSource` **`formula`** branch (previously a slice-2 deferral) now reads `source['expr']`, evaluates it via `const FormulaEvaluator().evaluate(expr, aspects)`, and returns the computed `num`. A blank/absent `expr` → a precise deferral reason; a thrown `FormulaException` is caught and turned into `formula "<expr>" failed: <message>` (still `deferred`, never a silent stat change). The class + `_resolveValueSource` doc comments updated to record that all three `modify_stat` value sources are now implemented.
  - **`tool/template_rule_resolver_harness.dart` (edited)** — the existing Tough-feat `formula` attachment (`2 * level`) now **resolves** (level 5 → **+10 max_hp**) instead of deferring; a NEW **Monk** attachment exercises the richer grammar — Unarmored Defense `10 + dex_mod + con_mod` = 10 + 2 + (−1) → **+11 unarmored_ac**, and a proficiency-bonus **`table(level, "1:2,5:3,9:4,13:5,17:6")`** at level 5 → **+3 prof_check**. Self-checks updated: stat overlay now expects `ac +4, speed +10, str +4, max_hp +10, unarmored_ac +11, prof_check +3` and **deferred drops 2 → 1** (only the `grant_refs` kind remains owed; the formula source no longer defers).
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve`; no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. With `fixed`/`field`/`formula` all done, `modify_stat` is complete; the next slices add the remaining rule **kinds** (`grant_refs`, `grant_proficiency`, `choose`, the pouch family, `check_clauses`, `note`), not new value sources.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: bracket balance exact on all three files (evaluator `{}`54/54 `()`173/173 `[]`16/16; resolver `{}`38/38 `()`87/87 `[]`21/21; harness `{}`41/41 `()`76/76 `[]`23/23, counted with line-comments/strings stripped); the formula semantics were **cross-checked with a Python port** (`2*level`→10, `10+dex_mod+con_mod`→11, `table(level,"1:2,5:3,…")`→3, `floor((9−10)/2)`→−1), all matching the harness expectations. Dart-specifics confirmed: the lexer's bare `continue;` inside `switch (c)` targets the enclosing `while` (not the switch) and is a valid case terminator; `arg.floor()`/`.ceil()` return `int` (a `num`); `num.tryParse('3')` yields an `int 3` so `prof_check == 3` holds; `library;` carries the file doc comment (satisfies `dangling_library_doc_comments`); the `formula_evaluator` import is consumed and `FormulaException`/`FormulaEvaluator` are library API (no unused-symbol lint). The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 4) — the first non-`modify_stat` rule kind: `note` + `check_clauses`.** With all three value sources done, begin the remaining rule kinds. Start with the two text-producing kinds that need no new infrastructure: **`note`** (emit `rule['text']` into `TemplateResolution.notes` when its trigger is active) and **`check_clauses`** (evaluate a small list of comparison clauses — e.g. `{aspect, op, value}` against the `AspectContext` — and push a typed string into `warnings` on failure, the §4.4-step-5 prereq-banner content). Extend the harness with a `note` attachment and a passing/failing `check_clauses` and assert `notes`/`warnings`. Then later slices: `grant_refs`/`grant_proficiency` (collection outputs), `choose` (pending-choice emission), and the pouch family (`set_pouch_max`/`refill_pouch`/`empty_pouch`/`grant_pouch`). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1/2/3 harness runs + `flutter analyze`.)*

## [14.06.2026 - 01:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 2: the `AspectContext` + the `field` value source)
- **Last Completed Task:** **PR-2.4 (slice 2) — the PC-card `AspectContext` is built (§4.3) and threaded through `resolve(...)`, and the resolver's `modify_stat` now evaluates a `field` value source.** Slice 1 stood up the shadow resolver with a constant (`fixed`) source only; this slice adds the second value source and the aspect map every later formula will read, still SHADOW-ONLY (no call-site flip — `character_resolver.dart` remains authoritative until Phase 3.11). One NEW file + two edited:
  - **`domain/services/template_rules/aspect_context.dart` (NEW)** — the immutable `aspectName → num` lookup of the-template-system §4.3, built purely from the PC card. Public surface: **`AspectContext`** (`{Map<String,num> aspects}` + `operator []`, `value(name)`→0-default, `has(name)`, `isEmpty`, and `const empty`) and the factory **`AspectContext.fromPcCard({EntityCategorySchema pcCategory, Map values, Map<String,int> classLevelsBySlug})`**. It walks the PC category's fields and publishes: (1) each **`abilityScoreTable`** with `typeConfig.publishAspects == true` → `<col>` (raw score) **and** `<col>_mod` = `floorDiv(score − modifierBase, modifierStep)` per column key *verbatim* (the string a formula must reference; `modifierBase` default 10, `modifierStep` default 2, step 0-guarded); (2) each **`combatStatsTable`** → exactly the three canonical aspects the doc names — `level`, `ac`, `max_hp` (other keys like `hp` are deliberately NOT published); (3) a plain **`integer`** field opting in via `typeConfig.publishAspect: "<name>"` (e.g. `prof_bonus`); (4) **`class_level(<slug>)`** for every entry in the caller-supplied `classLevelsBySlug` (the stored `class_levels` field is keyed by class *entity id*; only the caller has the entity table to map id→slug, so the context takes the already-resolved slug map — keeps the context pure and entity-free). **`_floorDiv` rounds toward −∞** (so a sub-base score like 9 → `floor((9−10)/2) = −1`, the doc's `floor()` semantics — Dart's `~/` would give 0); this is the resolver-side modifier, distinct from the widget's pixel-parity `~/` (the documented §4.3 vs §2.3 divergence). Numbers are coerced from `num`/numeric-`String`; anything else is skipped, never thrown.
  - **`domain/services/template_rules/template_rule_resolver.dart` (edited)** — `resolve(...)` gains an **`AspectContext aspects = AspectContext.empty`** param, threaded down through `_foldRule` to `_resolveValueSource` (the `fixed`/`field` sources don't need it; it is wired now so the fold signature is stable for slice 3's `formula`). **`_resolveValueSource` was rewritten to return `({num? value, String? reason})`** (so the caller records a *precise* deferral reason instead of a generic one) and now handles the **`field`** kind: `{"kind":"field","field":"<key>"}` reads `attachment.values[<key>]` coerced to a number; an **absent/blank `field` defaults to the rule's OWN field key** (so an ASI-style field can add its own stored value). A non-numeric/absent field value, a `formula` source, and an unknown kind each return a distinct `reason` → `deferred` (never a silent drop). The bare-number and `{"kind":"fixed"}` constant forms are unchanged. New private `static num? _coerceNum`.
  - **`tool/template_rule_resolver_harness.dart` (edited)** — `_field` helper gains `type`/`typeConfig` params. Two NEW attachments exercise the `field` source: a **Belt** (`value:{kind:field}` with no explicit key → reads its OWN `str_bonus`=4 → +4 str) and a **Cloak** (`value:{kind:field, field:'protection'}` → reads a *different* stored field =1 → +1 ac). A NEW **`AspectContext.fromPcCard`** block builds a synthetic PC card (an `abilityScoreTable` str/dex/con with `publishAspects`, a `combatStatsTable`, a `publishAspect` integer, `classLevelsBySlug:{barbarian:5}`) and **self-checks** str=16/str_mod=3/dex_mod=2/**con_mod=−1** (the floor() case)/level=5/ac=17/max_hp=42/prof_bonus=3/`class_level(barbarian)`=5/`!has('hp')`. The fold now expects **ac +4** (species 1 + shield 2 + cloak-field 1), **speed +10**, **str +4** (belt own-field), **deferred 2** (formula + grant_refs), and prints `Aspect self-check`, `Fold self-check`, and `Overall` PASS/FAIL lines.
  - **Scope / authority discipline:** SHADOW ONLY — no call site invokes `resolve`; no `rules` authored into the built-in (RULE RESET intact); no schema/hash change; no built-in asset regenerated. The `formula` value source and its `formula_evaluator.dart` (§4.3 grammar) are intentionally NOT built here — they are slice 3, and the `aspects` param is threaded specifically so that slice is a drop-in.
  - **Verification:** no Flutter toolchain in this env (`dart`/`flutter` absent) — verified statically: raw bracket counts balance on all three files (`aspect_context` `{}`18/18 `()`88/88 `[]`27/27; resolver `{}`40/40 `()`121/121 `[]`45/45; harness `{}`44/44 `()`94/94 `[]`22/22); `FieldSchema` named params used by the harness (`fieldId/categoryId/fieldKey/label/fieldType/orderIndex/typeConfig/rules/createdAt/updatedAt`) and `EntityCategorySchema` (`categoryId/schemaId/name/slug/fields/createdAt/updatedAt`) all confirmed present; `FieldType.{abilityScoreTable,combatStatsTable,integer}` exist; the relative import `aspect_context.dart` resolves (same dir as the resolver); the Dart-3 statement `switch` over `field.fieldType` with a `default: break;` is exhaustive-clean; the `({num? value, String? reason})` record return + `resolved.value!`/`resolved.reason` reads are type-correct; `_coerceNum`/`_floorDiv`/`_publish*` helpers are all consumed (no orphans); the threaded `aspects` param is unused by the two implemented sources but is not a lint under default `flutter_lints` (unused method params are not flagged) and is documented as slice-3 wiring. The `analyze` gate + `dart run tool/template_rule_resolver_harness.dart` must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 3) — the `formula` value source + `formula_evaluator.dart` (§4.3 grammar).** Add `template_rules/formula_evaluator.dart`: a ~150-line recursive-descent evaluator over the `AspectContext` — identifiers = aspect names (0 when absent), integer literals, `+ − * /`, `floor() ceil() min() max()`, `table(expr, "1:2,3:3,…")`, and parentheses (no conditionals — predicates become a `note`). Teach `_resolveValueSource`'s `formula` branch to evaluate `source['expr']` against the threaded `aspects` (replacing the slice-2 deferral), and extend the harness with a formula attachment (e.g. unarmored AC `10 + dex_mod + con_mod`, or `2 * level`) asserting the computed delta. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the slice-1/2 harness runs.)*

## [14.06.2026 - 00:00] - Last Update Status
- **Current Phase:** Template Rules Runtime (Roadmap Phase 3 — PR-2.4 / PR-T7, slice 1: the `TemplateRuleResolver` shadow skeleton + first rule kind)
- **Last Completed Task:** **PR-2.4 (slice 1) — the SHADOW `TemplateRuleResolver` stands up with its skeleton, the first rule kind (`modify_stat` + constant `fixed` value source), and a runnable dev harness.** Per the roadmap PR-T7 phasing (`lib/domain/services/template_rules/`, old engine still authoritative — no call-site flip until Phase 3.11), this lands the smallest end-to-end vertical slice so each later rule kind/value source can follow as a digestible, independently-verifiable increment. Two NEW files, both purely additive (nothing existing references them — the resolver is inert in the app build):
  - **`domain/services/template_rules/template_rule_resolver.dart` (NEW)** — the stateless fold runtime. Public surface: **`RuleTriggers`** (the closed 6-value trigger namespace — `when_granted`/`level_up`/`prereq_to_grant`/`when_equipped`/`prereq_to_equip`/`on_button`, with `fallback = when_granted` for an absent `trigger`, mirroring the v2 non-equippable default); **`RuleKinds`** (the closed 8-kind + `note` namespace); **`ResolverAttachment`** (`{entityId, EntityCategorySchema category, Map values, bool isEquipped}` — one rule-bearing contributor: class/subclass/species/background/feat/granted-trait/equipped-item, or the PC card itself); **`ResolverSkip`** (a deferred-rule record `{entityId, fieldKey, ruleIndex, kind, reason}` so an unbuilt kind/source is **surfaced, never silently dropped**); **`TemplateResolution`** (output overlay `{Map<String,num> statDeltas, List<String> notes, List<String> warnings, List<ResolverSkip> deferred}` + `delta(aspect)` reader — the in-progress `EffectiveCharacter`-equivalent of the-template-system §4.4 step 5); and **`TemplateRuleResolver.resolve(attachments, {gateLevel})`** — the deterministic fold (attachment order → field `orderIndex` → rule index, §4.4 step 3), a pure re-derive with no event log and no input mutation.
  - **The one implemented kind — `modify_stat` with a constant value (§4.2):** `_foldRule` reads `kind`; any non-`modify_stat` kind → `deferred` with reason. `_triggerActive` gates the three *folding* triggers (`when_granted` always; `when_equipped` only when `attachment.isEquipped`; `level_up` only when `gateLevel >= trigger_args.at_level` (default 1)); the non-folding triggers (`prereq_*`, `on_button`) on a `modify_stat` row are recorded as `deferred` (visibly unhandled), while an *inactive* folding trigger correctly contributes nothing (not a skip — e.g. an unequipped item's `when_equipped` AC). It then reads the `target` aspect path (empty → deferred) and resolves the value via **`_resolveValueSource`**, which evaluates ONLY a constant: a bare number (`"value": 2` shorthand) or `{"kind":"fixed","value":N}`. **`field`/`formula` sources return null → deferred** (they need the aspect context + the ~150-line formula evaluator of §4.3, the very next slices) so nothing is half-built. The resolved delta is summed into `statDeltas[target]`.
  - **`tool/template_rule_resolver_harness.dart` (NEW)** — a dev/CI script (`dart run tool/template_rule_resolver_harness.dart`, NOT in the app build; this is the only thing exercising the inert resolver until the PR-T7 debug panel lands). It builds a synthetic attachment set — a Defender species (`when_granted` +1 AC), an equipped shield (`when_equipped` +2 AC, bare-number shorthand), an **unequipped** plate (`when_equipped` +6 AC that must NOT apply), a barbarian (`level_up` +10 speed @L5 applies, +5 speed @L11 must NOT at `gateLevel:5`), and a Tough feat (a `formula`-valued `modify_stat` + a `grant_refs` kind, both expected in `deferred`) — folds at `gateLevel:5`, prints the overlay + deferred list, and **self-checks** (`ac==3 && speed==10 && deferred.length==2`, printing PASS/FAIL) so a regression fails loudly.
  - **Scope / authority discipline (roadmap §1.1, §1.4):** SHADOW ONLY — no call site invokes `resolve`; `character_resolver.dart` stays authoritative. No `rules` were authored into the built-in (it still ships rule-free per the RULE RESET — the exporter's invariant is untouched); the resolver only *interprets* `rules` that a future v3 entity carries. No existing file edited; no built-in asset regenerated; no schema/hash change.
  - **Verification:** no Flutter toolchain in this env — verified statically: bracket balance exact on both files (resolver `{}` 33/33, `()` 93/93, `[]` 37/37; harness `{}` 24/24, `()` 53/53, `[]` 15/15); the relative imports `../../entities/schema/{field_schema,entity_category_schema}.dart` resolve from `lib/domain/services/template_rules/` (`../../` → `domain/`), and `FieldType.integer` + the `FieldSchema`(`fieldId/categoryId/fieldKey/label/fieldType/orderIndex/rules/createdAt/updatedAt`) and `EntityCategorySchema`(`categoryId/schemaId/name/slug/fields/createdAt/updatedAt`) constructor args used by the harness all exist; every private helper (`_foldRule`/`_triggerActive`/`_isFoldingTrigger`/`_ruleAtLevel`/`_resolveValueSource`) is consumed (no orphans); the public `RuleTriggers`/`RuleKinds`/`TemplateResolution` members are library API (not unused-warning candidates); `gateLevel = 1 << 20` is a const-valid default; rule-map literals flow downward-inferred as `Map<String,dynamic>` from the typed `rules:` param. The `analyze` gate + the harness run must still be confirmed in a Flutter-capable env.
- **Next Task for Next Run:** **PR-2.4 (slice 2) — the aspect context + the `field` value source.** Add a `template_rules/aspect_context.dart` that builds the §4.3 aspect map from the PC card's own fields (each `abilityScoreTable` with `publishAspects` → `<col>`/`<col>_mod`; `combatStatsTable` → `level`/`ac`/`max_hp`; opt-in integer `publishAspect`; `class_level(<slug>)` from `class_levels`), thread it through `resolve(...)`, and teach `_resolveValueSource` the **`field`** kind (read `attachment.values[fieldKey]` as a number) so `modify_stat` can pull a stored field value. Hold the **`formula`** source (and its `formula_evaluator.dart`, §4.3) for slice 3. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the new harness run.)*

## [13.06.2026 - 23:00] - Last Update Status
- **Current Phase:** Built-in Template (Roadmap Phase 2 — PR-2.3 closeout, the last visual-parity gap: checkboxPouch `style:'pips'` renderer)
- **Last Completed Task:** **PR-2.3 (renderer follow-up) — `_SlotFieldWidget` (checkboxPouch) now honors `typeConfig.style:'pips'` + a fixed `countSource`, rendering the v3 death-saves / heroic-inspiration as clean tap-to-fill pips instead of the +/−/refill slot strip.** This closes the last visual-parity gap before the Phase-2 screenshot gate: slice B part 1 swapped those three integer fields to `checkboxPouch` with `typeConfig:{countSource:{kind:fixed,value:3}, style:'pips'}`, but the renderer ignored the hint and showed editable slot chrome — a visible regression from the v2 `_PipCounter`. All in **`presentation/widgets/field_widgets/field_widget_factory.dart`** (one widget, additive):
  - **`int? get _fixedPipCount`** — reads `schema.typeConfig`; returns the fixed pip count only when `style == 'pips'` **and** `countSource` is `{kind:'fixed', value:N>0}` (clamped 1..99), else `null`. Any other config (no `style`, `manual`/`field`/missing source) falls through to the unchanged default slot renderer, so editable pouches (and all v2 `slot` fields, which never carry this config) are untouched.
  - **`Widget _buildFixedPips(int count)`** — reuses the legacy **`_PipCounter`** verbatim for pixel-identical fill/border/size, wrapped in the standard `_LabeledFieldRow` (label-left, left-aligned pips). It derives `filled` = count of `true` states (clamped to the template's fixed count, which is authoritative over any stale stored `count`), and on tap converts `_PipCounter`'s cumulative int back to the canonical `{count, states:[true×n, false×(count−n)]}` wire via `_write`. Pips stay tappable regardless of `readOnly` (death saves / inspiration are mid-session updates — same policy as the v2 `_PipCounter`).
  - **`build`** now computes `_fixedPipCount` first and short-circuits to `_buildFixedPips` before the slot-strip path; the existing add/remove/refill renderer is otherwise unchanged (still serves variable-count pouches authored in the editor).
  - **Wire compatibility:** `_buildFixedPips` reads/writes the same `{count, states}` map as the slot path and as `migratePipIntToCheckboxPouch` (slice B part 1), so a migrated PC card (`{count:3, states:[…]}`) and a fresh/null value (→ 3 empty pips) both render and round-trip correctly. No `rules` touched; no built-in asset regenerated; v2 `dm.dart` schema untouched (dual-stack invariant).
  - **Verification:** no Flutter toolchain in this env — verified statically: bracket deltas vs HEAD are equal open/close for every type (`{}` +4/+4, `()` +23/+23, `[]` +6/+6 — the pre-existing −2 paren skew from comment/string text is unchanged), so the addition is self-balanced. `_PipCounter.onChanged` is `ValueChanged<int>` (the `(n)` lambda's `n` is `int`; `.clamp`/`List.generate` calls are int-typed); `_LabeledFieldRow`/`Align`/`Alignment` are in-scope (Material already imported); `schema.typeConfig` is the established `Map` accessor used across this file; `_fixedPipCount`/`_buildFixedPips` are both consumed in `build` (no orphans). The produced `{count,states}` matches `_parsed`'s reader, so the value round-trips.
- **Next Task for Next Run:** **Begin PR-2.4 — the `TemplateRuleResolver` shadow.** Stand up `lib/domain/services/template_rules/template_rule_resolver.dart` as a *dev/test-only* resolver that reads a v3 template's (future) `rules` and computes derived field values, with the **old hardcoded engine still authoritative** (no call-site flip — that's Phase 3.11). Start with the resolver skeleton + the simplest rule kind (a constant/`fixed` derivation) and a dev harness, so each later rule kind lands as a digestible slice. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; the `auditStaticFieldPreservation` assert then also runs against the loaded asset.)*

## [13.06.2026 - 22:00] - Last Update Status
- **Current Phase:** Built-in Template (Roadmap Phase 2 — PR-2.3 / PR-T6, slice B part 3: the STATIC-FIELD PRESERVATION CHECKLIST)
- **Last Completed Task:** **PR-2.3 (slice B, part 3) — the STATIC-FIELD PRESERVATION CHECKPOINT lands as an *enforced* invariant + a committed audit doc.** Slice B parts 1–2 swapped the PC category's mechanical fields to v3 types; this slice discharges the roadmap PR-2.3 requirement that every player-facing narrative/static-text field is provably **present and unchanged** in the v3 template (prompt §4 retention policy; roadmap §1.4 "static fields are sacred"). Two artifacts — code (the binding check) + doc (the human checklist):
  - **`domain/entities/schema/builtin/builtin_dnd5e_template_v3.dart`** — new `const staticNarrativeFieldTypes = {FieldType.text, textarea, markdown}` (a deliberate *superset* — protects by type, not by an allow-list of names, so a future category's prose field is covered automatically) and **`auditStaticFieldPreservation(WorldSchema v2, WorldSchema v3) → List<String>`**: it indexes the v3 fields by `(slug, fieldKey)`, then walks every `text`/`textarea`/`markdown` field of the **v2 source** and asserts the same key exists in v3 with an identical `fieldType` *and* a deep-equal `defaultValue` — throwing `StateError` naming the first dropped / retyped / value-mutated prose field, else returning every preserved `"<slug> / <fieldKey>"` id (the explicit audit list, *computed over the live schema so it can never drift from the source*). A small recursive `_deepValueEquals(a,b)` compares the JSON-shaped `defaultValue` (Map/List/scalar/null) by value, not reference. It is wired into `generateBuiltinDnd5eTemplateV3()` under an **`assert(() { auditStaticFieldPreservation(build.schema, v3); return true; }())`** — gates every debug/test build (same discipline as the editor's `typeConfig` validator) at **zero release cost**. **Holds by construction:** the v3 transform edits ONLY the PC category's mechanical fields (the six `pcFieldTypeSwaps` parity renames, the three `pcPipFieldKeys` int→checkboxPouch swaps, the three appended `actionButton` headers) — none of which is a text type — so every protected field is returned untouched by `_swapPcField`.
  - **`docs/new_system/static-field-preservation-checklist.md` (NEW)** — the committed PR checklist: §1 policy, §2 the enforcement mechanism (points at `auditStaticFieldPreservation` as the binding source of truth, explains why the invariant holds by construction), §3 the explicit per-tier/per-category audit list (`[x]` rows — Tier-0 lookups' shared `summary`+`effects` glossary pair + per-category extras; Tier-1 content prose incl. `class.multiclass_*`, `feat.prerequisite`/`benefits`, `spell.description`, `magic-item` sentient/charge text, `trait`/`creature-action` descriptions; Tier-2 DM prose with **`player-character` called out as the safety-critical row** — its 8 markdown + 6 identity-text fields all sit outside the swap sets), §4 the Phase-4 re-audit hook (automatic, since the check is the live assertion not a frozen snapshot).
  - **Static-field source of truth:** the per-category enumeration was derived by sweeping `lookups.dart`/`content.dart`/`dm.dart` (the `_FB` builder's `fb.text`/`fb.textarea`/`fb.markdown` helpers = `FieldType.{text,textarea,markdown}`). The doc is the human-readable companion; the code assertion is authoritative and catches any future drop even if the doc misses a field.
  - **RULE RESET + scope discipline preserved.** No `rules` added; no built-in asset regenerated; the v2 schema (`dm.dart` shared `_playerCharacterCategory`) untouched (dual-stack invariant, roadmap §1.4). Audit-only — no behavior change on any sheet.
  - **Verification:** no Flutter toolchain in this env — verified statically: bracket balance exact on the generator (`{}` 65/65, `()` 150/150, `[]` 50/50); `FieldType` is an `enum` (`.name` valid) and `FieldSchema.defaultValue` / `EntityCategorySchema.slug` / `.fields` confirmed in `field_schema.dart`/`entity_category_schema.dart`; `auditStaticFieldPreservation` (3 refs — def + generator call + doc), `staticNarrativeFieldTypes` (2 — def + use), `_deepValueEquals` (4 — def + use + 2 doc) are all actively consumed (no orphans); the `assert(() {…}())` form is analyze-clean and release-stripped; PC narrative fields (`backstory`/`ideals`/`bonds`/`flaws`/`appearance`/`trinket`/`personality_traits`/`allies_organizations` = `fb.markdown`; `age`/`height`/`weight`/`eyes`/`skin`/`hair` = `fb.text`) confirmed absent from `pcFieldTypeSwaps`/`pcPipFieldKeys`, so the assertion passes.
- **Next Task for Next Run:** **PR-2.3 closeout → Phase-2 wrap.** With parity renderers, the PC type swap, the action-button hoist, and the static-field checkpoint all landed, PR-2.3 is functionally complete. Next: the recommended renderer follow-up — teach `_SlotFieldWidget` (checkboxPouch) in `field_widget_factory.dart` to honor `typeConfig.style:'pips'` + a fixed `countSource` so the v3 death-saves/heroic-inspiration render as clean tap-to-fill **pips** (like the v2 `_PipCounter`) instead of +/− slot buttons — closing the last visual-parity gap before the Phase-2 screenshot gate. Then begin **PR-2.4** (`TemplateRuleResolver` shadow at `lib/domain/services/template_rules/`, dev-test-copy only, old engine still authoritative). *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; the new `auditStaticFieldPreservation` assert will then run against the loaded asset too.)*

## [13.06.2026 - 21:00] - Last Update Status
- **Current Phase:** Built-in Template → PC-category v3 type swap (Roadmap Phase 2 — PR-2.3 / PR-T6, slice B part 2: `actionButton` header fields + retire `_renderRestActions`)
- **Last Completed Task:** **PR-2.3 (slice B, part 2) — the three `actionButton` header fields land on the v3 PC category and the hardcoded `_renderRestActions` row is schema-driven.** Slice 3b wired the `actionButton` *renderer* + the `onAction` → `_runCardAction` seam but had nothing to hoist (no built-in field used the type); this slice ships the fields and flips the sheet header to render them from the schema. Two files:
  - **`domain/entities/schema/builtin/builtin_dnd5e_template_v3.dart`** — new `const pcActionButtonFields` (a `({String key, String label, String action})` record list: `level_up`/`short_rest`/`long_rest`, labels matching the legacy button text verbatim). `swapPlayerCharacterFieldTypes` now delegates the PC category's fields to a new **`_swapAndAppendPcFields(categoryId, fields)`**: it runs the per-field `_swapPcField` (unchanged) then **appends** the three buttons as `FieldType.actionButton` fields with `typeConfig:{action, placement:'header'}`, deterministic `fieldId`s (`fld-pc-<action>`), `isBuiltin:true`, `orderIndex` after the last field, and the frozen `builtinDnd5eTemplateTimestamp` (byte-identical export). **Idempotent** — a button whose `fieldKey` already exists is skipped, so re-evolving an already-v3 category is a no-op. The `typeConfig` shape is **byte-identical to `defaultTypeConfig(FieldType.actionButton)`** (`{action:'level_up', placement:'header'}`) and the action values match `actionButtonActions`, so a copied built-in passes `_validateTypeConfig` and opens immediately editable. **RULE RESET honored** — `typeConfig` only, never `rules` (what a button does to a pouch is an `on_button` rule on the target pouch field, a Phase-3 JIT wave).
  - **`presentation/screens/characters/character_editor_screen.dart`** — the sheet body's `_renderRestActions(...)` call is replaced by a new **`_renderHeaderActions(palette, playerCat, character)`**: it collects every PC-category field with `fieldType == actionButton && typeConfig['placement'] == 'header'` (sorted by `orderIndex`) and renders them in a `Row` of equal-width `Expanded` `OutlinedButton.icon`s — **pixel-identical** layout/icons (`arrow_upward`/`bedtime_outlined`/`nightlight_round`) to the legacy row — each firing `_runCardAction(action, character)`. **Dual-stack fallback (roadmap §1.4):** when no header `actionButton` field exists (any v2 schema), it returns the **unchanged** `_renderRestActions` row, so no existing world loses its rest buttons mid-migration. `_renderRestActions` is kept and re-documented as the v2-only fallback. `_renderSchemaFields` now **skips** header `actionButton` fields inline (they're hoisted), so they never double-render. New helpers `_headerActionIcon`/`_headerActionLabel` map the verb → glyph/fallback-label, identical across the header row and the inline `_ActionButtonFieldWidget`.
  - **Verification:** no Flutter toolchain in this env — verified statically: bracket balance — generator `{}`/`()`/`[]` all exactly even (40/40, 106/106, 37/37); editor deltas vs HEAD unchanged (`(` +1 and `]` +1 pre-existing comment/string skews are byte-for-byte identical before/after), so every addition is self-balanced. `FieldType.actionButton` exists; `FieldSchema(...)` required args all supplied (`fieldId/categoryId/fieldKey/label/fieldType/createdAt/updatedAt`) + optional `typeConfig/isBuiltin/orderIndex`; record-list literal is const-valid (all-String const fields). `EntityCategorySchema.categoryId`/`.fields` confirmed; the fold accumulator `max` doesn't shadow `dart:math` (not imported). Editor uses only already-imported symbols (`FieldType`/`IconData`/`Icons`/`Expanded`/`Row`/`OutlinedButton`/`SizedBox`/`EntityCategorySchema`); `_runCardAction` returns `Future<void>` assigned to `onPressed` exactly as the legacy row does (`() => _levelUp(...)`). `actionButtonActions` + `defaultTypeConfig(actionButton)` byte-match the produced config. No committed v3 asset, so the loader hash-assert falls back to the generator — the hash delta is inert until the PR-1.3 export runs.
- **Next Task for Next Run:** **PR-2.3 (slice B, part 3) — the STATIC-FIELD PRESERVATION CHECKLIST** (roadmap PR-2.3): an explicit per-category audit of every narrative/markdown/static-text field in the built-in (`backstory`, `personality_traits`, `ideals`, `bonds`, `flaws`, `appearance`, `biography`, `notes`, the multiclass narrative markdowns, …), asserted present + unchanged in the v3 template, committed as the PR checklist (retention policy, prompt §4). *Recommended renderer follow-up (still open):* teach `_SlotFieldWidget` (checkboxPouch) to honor `style:pips` + a fixed `countSource` so the v3 death-saves render as clean tap-to-fill pips (like the v2 `_PipCounter`) instead of +/- slot buttons. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [13.06.2026 - 20:00] - Last Update Status
- **Current Phase:** Built-in Template → PC-category v3 type swap (Roadmap Phase 2 — PR-2.3 / PR-T6, slice B part 1: value-migrating pip swap + the converter shim)
- **Last Completed Task:** **PR-2.3 (slice B, part 1) — the three death-save / heroic-inspiration `integer` fields swapped to `checkboxPouch` in the v3 generator, with the shared value-migration shim (content-convert §2).** Slice A landed the wire-identical parity renames; this lands the first *value-migrating* swap — the half that needs a card-value migration so neither the template nor stored content is half-built. Two files:
  - **`domain/services/template_migration/legacy_content_converter.dart` (NEW)** — the canonical converter module mandated by content-convert §Tooling (the *same* code is meant to run in the offline pack CLI, the on-open personal-package shim, and the world-entity v3-migration prompt). This is its **first slice**: the `migratePipIntToCheckboxPouch(value, {count})` shim that maps the stored int `n` → `{count, states:[true×n, false×(count−n)]}`. It is **deterministic and idempotent** (content-convert §Verification) — `_coercePipFilled` accepts the bare-int v2 form, the canonical `{count,states}` map (counts `true`s — re-running is a byte-identical no-op), and the legacy `{count,filled}` map; null/garbage → an all-`false` pouch (the field's zero value), never an exception. Template-agnostic (the caller passes `count`), so it serves the built-in PC sheet and any custom template adopting the type. The remaining per-kind mappings (content-convert §3-7) are deferred to the bundled-pack conversion wave (PR-T9); only `migratePipIntToCheckboxPouch` ships now (no dead helpers — a `resolveCheckboxPouchCount` I drafted was removed because its only consumer, the not-yet-wired world-open shim, lands in PR-T9).
  - **`domain/entities/schema/builtin/builtin_dnd5e_template_v3.dart`** — the swap step now also rewrites the pip fields. New `pcPipFieldKeys = {death_saves_successes, death_saves_failures, heroic_inspiration}`, `pcPipCount = 3`, and `pcPipTypeConfig = {countSource:{kind:fixed,value:3}, style:pips}` (mirrors `defaultTypeConfig(checkboxPouch)` and passes the editor's `_validatePouchSource`, so a copy opens immediately editable). `swapPlayerCharacterFieldTypes` was refactored to delegate each field to a new `_swapPcField(FieldSchema)`: parity keys ([pcFieldTypeSwaps]) rewrite only type+typeConfig (unchanged), while pip keys additionally **migrate `defaultValue` through the shim** (`0` → `{count:3, states:[false,false,false]}`) and **clear the integer-only `minValue`/`maxValue` validation** (reset to `const FieldValidation()` — the v2 `0..3` bound is meaningless on a map value; field-cleanup policy). Idempotent end-to-end: the generator rebuilds from the frozen v2 schema each call, and the shim re-migrates an already-pouch value byte-identically.
  - **Dual-stack discipline preserved (roadmap §1.4).** The transform stays in the v3 generator — `dm.dart`'s shared `_playerCharacterCategory` (consumed by the *frozen v2 schema* too) is untouched, so existing v2 worlds keep their `integer` death-save fields and the `_PipCounter` int renderer (`field_widget_factory.dart:1101`). On a future v3 world the same fields are `checkboxPouch` → `_SlotFieldWidget` (slice-1 alias); both render correctly against their own wire. No v3 world exists in production yet (the authority flip is Phase 3.11), so nothing renders the v3 pouch death-saves today — the swap is inert until a v3 world is created/migrated.
  - **RULE RESET honored.** The pip swap attaches `typeConfig` ONLY — never `rules` (roadmap §1.1; the exporter's rule-free invariant still holds). Static/narrative fields remain untouched (retention policy).
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren/bracket balance OK on both files (33/33, 78/78, 28/28 and 19/19, 37/37, 13/13); the relative import `../../../services/template_migration/legacy_content_converter.dart` resolves (builtin/ → schema → entities → domain → services); `FieldSchema.copyWith(fieldType:,typeConfig:,defaultValue:,validation:)` and `FieldValidation()` are freezed-valid (validation is the existing import); `clamp(0,count)` returns `int` (int receiver + int args — matches `_SlotFieldWidget._parsed`'s existing `(m['count'] as num?)?.toInt().clamp(0,99)`); `pcPipTypeConfig` is const-valid (all-const nested literals) and satisfies `_validateTypeConfig`'s checkboxPouch arm (`_validatePouchSource(countSource,'count')`); `migratePipIntToCheckboxPouch` is actively consumed (defaultValue migration — not orphaned); the `library;` directive carries the file-level doc comment (satisfies `dangling_library_doc_comments`). No committed v3 asset exists, so the loader's hash-assert falls back to the generator and the hash delta is inert until the PR-1.3 export runs.
- **Next Task for Next Run:** **PR-2.3 (slice B, part 2) — the `actionButton` header fields + retire `_renderRestActions`.** In `builtin_dnd5e_template_v3.dart`'s swap step add the three `actionButton` fields (`level_up`/`short_rest`/`long_rest`, `typeConfig:{action, placement:'header'}`) to the PC category, and in `character_editor_screen.dart` retire the hardcoded `_renderRestActions` row, hoisting the buttons into the sheet header (the `onAction` seam from slice 3b already drives `_runCardAction` → the same `_levelUp`/`_shortRest`/`_longRest` handlers). Then **slice B, part 3 — the STATIC-FIELD PRESERVATION CHECKLIST** (roadmap PR-2.3): an explicit per-category audit of every narrative/markdown field, asserted present in the v3 template, committed as the PR checklist. *Recommended renderer follow-up:* teach `_SlotFieldWidget` (checkboxPouch) to honor `style:pips` + a fixed `countSource` so the v3 death-saves render as clean tap-to-fill pips (like the v2 `_PipCounter`) instead of the current +/- slot buttons. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

### Earlier history
## [13.06.2026 - 19:00] - Last Update Status
- **Current Phase:** Built-in Template → PC-category v3 type swap (Roadmap Phase 2 — PR-2.3 / PR-T6, parity-rename slice)
- **Last Completed Task:** **PR-2.3 (PC-category parity type swap, slice A) — the Player Character category's wire-identical v2 field types migrated to their v3 equivalents, in the v3 generator only.** With every v3 renderer now live on the PC sheet (slices 1–3b), the built-in template itself can begin adopting the v3 types. This slice lands the **value-preserving** half (content-convert §1 — renames that need **zero** card migration); the genuinely-new conversions that require a value migration are held for slice B (below). All changes in **`domain/entities/schema/builtin/builtin_dnd5e_template_v3.dart`**:
  - **Dual-stack discipline — transform lives in the v3 generator, NOT in `dm.dart`.** The PC category is defined in the *shared* `dm.dart` `_playerCharacterCategory`, consumed by **both** the frozen v2 schema (still authoritative for existing worlds on the old engine) and the v3 template. Editing `dm.dart` would mutate the v2 engine and violate the dual-stack invariant (roadmap §1.4). Instead, `generateBuiltinDnd5eTemplateV3()` now applies a new **`swapPlayerCharacterFieldTypes(WorldSchema)`** step *after* the in-place v2→v3 evolution: it finds the category by `slug == 'player-character'` (new const `builtinPlayerCharacterSlug`, robust to the generated `categoryId`) and rewrites only its fields. Every other category and field is returned untouched; the function is idempotent and value-preserving (no `defaultValue`/`subFields` — the stored value wire — is altered).
  - **The six swaps** (new const `pcFieldTypeSwaps`: `fieldKey → (FieldType, typeConfig)`), each carrying the `typeConfig` the v3 renderers + the PR-2.2b editor forms read (shapes mirror `defaultTypeConfig` in `template_editor_provider` and satisfy its completeness validator, so a copy of the built-in opens immediately editable): `stat_block` **statBlock→abilityScoreTable** (`columns` use **UPPERCASE** `STR…CHA` keys to match the stored `{"STR":10,…}` wire verbatim → `_resolveAbilityColumns` returns them, `modifierBase:10`/`modifierStep:2` reproduce today's `(score-10)/2` exactly); `combat_stats` **combatStats→combatStatsTable** (`visibleKeys` = the seven canonical PC stats; the monster-only `cr` sub-field is dropped from the *visible* set but the stored value map is untouched, and the renderer still reads `subFields` for parity); `saving_throws`/`skills`/`class_resources` **proficiencyTable→skillTree** (`tiers:['proficient']` for saves — no expertise in 5e — vs `['proficient','expertise']` for skills/resources; rows `{name,ability,proficient,expertise,misc}` reused unchanged); `spell_slots` **spellSlotGrid→pouchMatrix** (`rowKeys:['1'..'9']`, `rowLabelPrefix:'Level '`, `maxSource:{kind:manual}`; the renderer derives rows from `max.keys` so `rowKeys` is advisory; `manual` keeps the field rule-free until a Phase-3 levelTable/formula lands).
  - **RULE RESET preserved + static-field retention honored.** Every swap attaches `typeConfig` ONLY — never `rules` (roadmap §1.1; the exporter's rule-free invariant still passes). The swap touches **only** the six mechanical fields; all narrative/static text fields (`backstory`, `personality_traits`, `ideals`, `bonds`, `flaws`, `appearance`, `allies_organizations`, `trinket`, the multiclass narrative markdowns, …) are left exactly as-is — the field-cleanup/retention policy is respected.
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren/bracket balance OK; the `const Map<String, ({FieldType type, Map<String, dynamic> typeConfig})>` record-map is const-valid (all nested map/list/scalar literals are const in a const context); `FieldType.{abilityScoreTable,combatStatsTable,skillTree,pouchMatrix}` confirmed present in `field_schema.dart`; `WorldSchema.copyWith(categories:)`, `EntityCategorySchema.copyWith(fields:)`, `FieldSchema.copyWith(fieldType:,typeConfig:)` all freezed-valid; the unused `entity_category_schema.dart` import was removed (types are inferred — only `FieldType` is named, so only `field_schema.dart`/`world_schema.dart` are imported) to keep `flutter analyze` clean; the produced `typeConfig`s satisfy `template_editor_provider`'s `_validateTypeConfig` (columns unique/non-empty + step≠0; visibleKeys ⊆ `combatStatsCanonicalKeys`; tiers non-empty; rowKeys non-empty + valid `maxSource`). No consumer asserts a fixed v3 hash — the loader computes at runtime and (with no asset committed yet) falls back to the generator, so the hash delta is inert until the PR-1.3 export runs.
- **Next Task for Next Run:** **PR-2.3 (PC-category type swap, slice B) — the value-migrating conversions + `actionButton` header fields.** In `builtin_dnd5e_template_v3.dart`'s swap step: (1) convert the three integer pip fields — `death_saves_successes`, `death_saves_failures` (max 3), `heroic_inspiration` (max 3) — from `integer` to **`checkboxPouch`** (`countSource:{kind:fixed,value:3}`, `style:pips`), and write the **world-open value-migration shim** that maps the stored int `n` → `{count:3, states:[true×n, false×(3−n)]}` (content-convert §2) so existing PC cards don't break; (2) add the three **`actionButton`** fields (`level_up`/`short_rest`/`long_rest`, `placement:header`) and **retire the hardcoded `_renderRestActions` row** in `character_editor_screen.dart` (the `onAction` seam from slice 3b already drives the same handlers), hoisting them into the sheet header per the `placement` hint. Then the **STATIC-FIELD PRESERVATION CHECKLIST** (roadmap PR-2.3): an explicit audit list of every narrative/markdown field per built-in category, asserted present in the v3 template, committed as the PR checklist. *(Still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [13.06.2026 - 18:00] - Last Update Status
- **Current Phase:** Template Editor UI → Parity Renderers (Roadmap Phase 2 — PR-2.3 slice 3b, `actionButton` renderer on the PC sheet)
- **Last Completed Task:** **PR-2.3 (slice 3b) — `actionButton` renderer wired into the PC-sheet `FieldWidgetFactory` via a player-action callback seam.** This was the held "harder half" of slice 3: the Level Up / Short Rest / Long Rest verbs. Changes span two files:
  - **`presentation/widgets/field_widgets/field_widget_factory.dart`** — `FieldWidgetFactory.create` gains an optional **`Future<void> Function(String action)? onAction`** parameter (documented), and a new explicit `FieldType.actionButton` switch arm (was falling through to the raw-text `_` default) routing to the new **`_ActionButtonFieldWidget` (StatelessWidget)**. The widget reads `typeConfig.action ∈ {level_up, short_rest, long_rest}` (defaults to `level_up` on a missing/unknown value so a malformed config still renders a usable verb), renders a full-width `OutlinedButton.icon` whose **label is the creator-editable `FieldSchema.label`** (falling back to the matching verb text — "Level Up"/"Short Rest"/"Long Rest" — when blank, so it mirrors the hardcoded `_renderRestActions` row) with the per-action icon (`arrow_upward`/`bedtime_outlined`/`nightlight_round`, identical to the legacy row). On press it fires `onAction!(action)`. When `onAction` is **null** (e.g. the database card view, which has no live `Character`) the button renders **disabled** rather than half-working. Carries no card value — `actionButton` stores nothing.
  - **`presentation/screens/characters/character_editor_screen.dart`** — `_fieldTile`'s `FieldWidgetFactory.create(...)` call now passes `onAction: (action) => _runCardAction(action, character)`, and a new **`_runCardAction(String action, Character character)`** dispatcher maps `level_up`/`short_rest`/`long_rest` onto the existing State handlers `_levelUp`/`_shortRest`/`_longRest` (unknown action → no-op `Future.value()`). So the new field button and the legacy `_renderRestActions` row drive the **same** code path.
  - **Architectural decision — callback seam, NOT a `character_actions_service.dart` extraction (deviation from the prior Next-Task plan, by design):** the handlers (`_levelUp`/`_shortRest`/`_longRest`/`_maybeRunLevelUp`) are *irreducibly* State-bound — they close over `context`, `ref`, `mounted`, `setState`, `_mutate`, and three private dialogs (`LevelUpDialog`, `_ShortRestDialog`, `_LevelUpClassPicker`) plus `_resolvePendingChoice`. A "service" would have to take every one of those back in as parameters, and its only consumer (the PC sheet) lives **inside** that same State — so the extraction would add churn (moving/​publicizing private dialogs) and risk with zero consumer benefit. The doc's stated intent for the service ("the editor and the new widget share one path") is delivered exactly by the `onAction` callback: one seam, one path, no dead service layer. The renderer/factory layer stays the only thing touched — **no resolver/runtime change** (Phase 2.4/3), **no built-in asset regeneration**.
  - **Scope discipline — `placement` deferred (honestly, not half-built):** `typeConfig.placement` (`header`/`inline`) is a host *layout* hint. Inline rendering (the widget in its field-list slot) is always correct and fully functional today. `header` hoisting — lifting these buttons into the sheet header — pairs with the **built-in PC-category type swap** that retires the hardcoded `_renderRestActions` row (no built-in field uses `actionButton` yet, so there is nothing to hoist now); it lands with that swap so neither side ships a stub. Documented in the widget's doc-comment.
  - **Verification:** no Flutter toolchain in this env — verified statically: bracket-balance by **delta vs HEAD** on both files — `{`/`}` deltas equal (+7 factory, +2 editor) and `(`/`)` deltas equal (+32 factory, +10 editor), and the two **pre-existing** skews (factory `)` +2 over `(`; editor `(` +1 over `)`) are **unchanged**, so every addition is self-balanced. `FieldType.actionButton` exists in `field_schema.dart` and now appears exactly once in the switch (explicit arm before `_`, no duplicate). The widget uses only Material symbols already imported (`OutlinedButton`/`Icon`/`Icons`/`Padding`/`SizedBox`/`Text`/`EdgeInsets`/`IconData`) and valid `FieldSchema` accessors (`typeConfig`, `label`); no `DmToolColors` member referenced (plain `OutlinedButton.icon`, matching `_renderRestActions`). Callback types line up end-to-end (`Future<void>` from `_runCardAction` → `_levelUp`/`_shortRest`/`_longRest`). The unrelated `actionButton` local in `hub/characters_tab.dart:351` is a `FilledButton` variable, untouched. Other `FieldWidgetFactory.create` call sites are unaffected (`onAction` is optional → null → disabled button, the correct non-character behavior).
- **Next Task for Next Run:** **PR-2.3 closeout / Phase-2 wrap → begin the built-in PC-category type swap (roadmap PR-T6).** All v3 parity + new-type renderers (`abilityScoreTable`, `combatStatsTable`, `checkboxPouch`, `pouchMatrix`, `skillTree`, `levelMatrix`, `intPouch`, `recordList`+presets, `levelUpTable`, `actionButton`) are now live on the PC sheet, so the renderer layer no longer blocks the swap. Next, in a Flutter env: (1) **PR-1.3 closeout** — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; (2) swap the built-in PC category's v2 field types to their v3 equivalents (e.g. `statBlock`→`abilityScoreTable` with the SRD `columns`/base/step config, `slot`→`checkboxPouch` for death saves/heroic inspiration, `spellSlotGrid`→`pouchMatrix`, `proficiencyTable`→`skillTree`), add `actionButton` fields for the three rest verbs (with `placement: header`) and retire the hardcoded `_renderRestActions` row, and write the world-open value migration (death-save ints → `{count,states[]}`). Keep the rule runtime for Phase 3. *(`actionButton` header-placement hoisting lands with this swap.)*

## [13.06.2026 - 17:00] - Last Update Status
- **Current Phase:** Template Editor UI → Parity Renderers (Roadmap Phase 2 — PR-2.3 slice 3a, `levelUpTable` renderer on the PC sheet)
- **Last Completed Task:** **PR-2.3 (slice 3a) — `levelUpTable` renderer wired into the PC-sheet `FieldWidgetFactory`.** Slice 2 left the two genuinely-new tabular types (`actionButton`, `levelUpTable`) on the raw-text default. This slice lands `levelUpTable` — the one that needs **no service extraction or runtime change** (pure data-authoring/display). All in **`presentation/widgets/field_widgets/field_widget_factory.dart`**:
  - **`levelUpTable` → new `_LevelUpTableFieldWidget` (StatelessWidget)** + switch arm (passes `entities` for ref-name display). Renders the the-template-system §2.3 wire — `List<{level:int, description, grants:[{ref,target}], choices:[{choiceId,prompt,pick,optionRefs[],target}]}]`. **Read mode:** sorted-by-level compact view — an `L<n>` accent badge per row, the description, then `• Grants <name> → <target>` bullets and `◆ <prompt> (pick N of M)` choice lines (entity refs resolved to names via the entity map, `_levelUpRefName`); hidden entirely when empty. **Edit mode:** insertion-order (no live-edit reorder so the cursor never jumps), per-row Level + description fields with a delete-row control, and **nested add/remove sub-lists** for grants ({ref, target} text fields) and choices ({prompt, pick, target} + an add/remove `optionRefs` list). "+ Add level" seeds the next level number; new choices mint a `choiceId`. `typeConfig.gate` (`class`/`character`, from the PR-2.2b form) surfaces as a "Gates on <x> level" caption — it only steers the Phase-3 resolver, not this UI. New helpers: top-level `_levelUpRefName`, and a small `_SubListHeader` widget for the Grants/Choices section rows.
  - **Scope discipline — why `actionButton` is held for slice 3b:** the `levelUpTable` rows *gate* level-up grants, but firing them is the Phase-3 rule runtime's job (`planLevelUp`'s rewrite) — so this renderer stays pure authoring/display with **no runtime change**, exactly like slice 2's `intPouch`/`recordList`. `actionButton` is the harder half: it must invoke the rest/level-up handlers, which today are **State-bound** to `character_editor_screen.dart` (`_levelUp`/`_shortRest`/`_longRest`/`_maybeRunLevelUp` all close over `_mutate`, `context`, `ref.watch`, `mounted`, dialogs). Extracting those into a reusable service **and** threading a new action callback (plus the live `Character`) through `FieldWidgetFactory.create` and every call site is a genuinely larger, higher-risk multi-file refactor — split into its own slice so each step ships complete and placeholder-free. No built-in field uses `actionButton` yet, so it stays on the existing text fallback rather than rendering half-built. Refs are authored as plain id/name strings (the documented wire) — the full entity picker lands with the JIT class wave (Phase 3, Wave 6) that first ships a populated `levelUpTable`.
  - **Verification:** no Flutter toolchain in this env — verified statically: `{}`/`[]` balance exact; the `()` skew is **−2 in HEAD already** (pre-existing comment/string text) and **unchanged** after the edit, so the additions are paren-balanced; `FieldType.levelUpTable` exists in `field_schema.dart` and appears exactly once in the switch (no duplicate arm; `actionButton` deliberately absent → text default); every referenced `DmToolColors` member (`featureCardAccent`, `featureCardBorder`, `featureCardBg`, `srdSubtitle`, `br`) confirmed present; all symbols (`Entity`, `Colors`, `DateTime`) already imported; the edit-mode `TextFormField`s use `initialValue:` + per-row/sub-index `ValueKey`s (the slice-2 `_RecordListFieldWidget` idiom — unique keys, no controller lifecycle).
- **Next Task for Next Run:** **PR-2.3 slice 3b — `actionButton` renderer with rest/level-up service extraction.** (1) Extract the State-bound handlers from `character_editor_screen.dart:2755+` (`_levelUp`, `_shortRest`, `_longRest`, `_maybeRunLevelUp` + their `_readHp`/`_writeHp`/`_hitDieMax`/`_conModifier`/`_readClassLevels`/`_readAbilityScores` helpers) into a small `application/services/character_actions_service.dart` that takes the `Character` + a `BuildContext` + mutate callback and owns `planLevelUp`/short-rest/long-rest, so the editor and the new widget share one path. (2) Thread an `onAction(String action)` callback (and the live `Character`/level) through `FieldWidgetFactory.create` + its PC-sheet call site. (3) Build `_ActionButtonFieldWidget` — renders the FieldSchema label as a button invoking the service per `typeConfig.action ∈ level_up|short_rest|long_rest`, honoring `placement` where the sheet exposes header slots. Keep resolver/rule changes for Phase 2.4/3. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the built-in PC-category type swap to the v3 parity types once all renderers are live.)*

## [11.06.2026 - 07:00] - Last Update Status
- **Current Phase:** Template Editor UI → Parity Renderers (Roadmap Phase 2 — PR-2.3 slice 2, new-type renderers on the PC sheet)
- **Last Completed Task:** **PR-2.3 (slice 2) — `intPouch` renderer + `recordList` preset routing wired into the PC-sheet `FieldWidgetFactory`.** Slice 1 aliased the six *parity* v3 types to their v2 ancestors; the genuinely-new types still fell through to the raw-text default (`_ => _TextFieldWidget`). This slice lands the two that need **no service extraction or runtime change** — fully self-contained renderer-layer work. The `actionButton` (needs the rest/level-up handlers extracted from `character_editor_screen.dart:2598+` into a service) and `levelUpTable` are deliberately held for slice 3. All changes in **`presentation/widgets/field_widgets/field_widget_factory.dart`**:
  - **`intPouch` → new `_IntPouchFieldWidget` (StatefulWidget).** Renders the `{current, max}` wire (the-template-system §2.3) as a "current / max" header with `−`/`+` (spend/recover, clamped to `[0, max]`), **refill** (snap current→max) and **empty** (→0) buttons. `maxSource.kind: manual` (the default — `maxSource` absent ⇒ manual) gives the DM an inline editable max `TextFormField` (seeded once in `initState`, reconciled in `didUpdateWidget`); the `fixed`/`levelTable`/`formula` kinds — resolved only by the Phase-3 rule runtime — show the stored max **read-only** so the field is fully usable rule-free today and no card breaks. `_write` always clamps current to the ceiling so a lowered max can't strand an over-full pouch. Styled to match `_SlotFieldWidget` (same compact 28px icon buttons, `featureCardAccent` for the current value).
  - **`recordList` → `_buildRecordListField` dispatcher.** Routes by `typeConfig.preset` to the bespoke v2 renderers **verbatim** (byte-identical parity, the-template-system §3): `ranged-senses`→`RangedSenseListFieldWidget`, `equipment-choices`→`EquipmentChoiceGroupsFieldWidget`, `subspecies-options`→`SubspeciesOptionsFieldWidget`; the rule-authoring presets `spell-effects`/`prereq-clauses` render `SizedBox.shrink()` exactly as their v2 FieldTypes (`spellEffectList`/`prereqClauses`) do post-Phase-1.1. **No preset → new generic `_RecordListFieldWidget`**: reads `typeConfig.columns` (`{key,label,kind}`) and renders one card per row with a per-column editor keyed off `kind` — `text`/`dice` (text field), `int`/`float` (numeric, null on empty), `bool` (Switch), `enum` (Dropdown over `options`, guarded against a stale value), `ref` (read-mode resolves a hard `{ref|lookup|slug}` against the entity map for display; edit-mode is a soft-ref `{name, slug}` text field) — plus add/delete-row controls. Rows stored as `List<Map>` keyed on column `key`. Helpers added: `_RecordColumn`, `_resolveRecordColumns`, `_recordSlugify`, `_RecordCellLabel`.
  - **Scope discipline:** renderer/factory layer only — **no resolver/runtime change** (Phase 2.4/3), **no built-in asset regeneration**. The generic recordList's `ref`-column editor uses a soft-ref text field rather than the full entity picker; this is honest and round-trips, and the full picker is deferred to the JIT wave (Phase 3, Wave 5 — feat `asi_options`) that first ships a ref-bearing **preset-less** recordList. No built-in card uses one yet (the parity presets cover all current ref-bearing lists), so nothing is half-built in production.
  - **Verification:** no Flutter toolchain in this env — verified statically: `{}`/`[]` balance exact; the `()` skew is **+2 in HEAD already** (pre-existing comment/string text) and **unchanged** after the edit, so the additions are paren-balanced; each `FieldType` case appears exactly once in the switch (no duplicate `intPouch`/`recordList` arms); `field_type_meta.dart` already carries both types (44-type coverage from PR-1.5); `DropdownButtonFormField` uses `initialValue:` (codebase convention); every referenced `DmToolColors` member (`featureCardAccent`, `featureCardBorder`, `srdSubtitle`, `br`) confirmed present; preset widget constructors (`RangedSenseListFieldWidget`/`EquipmentChoiceGroupsFieldWidget`/`SubspeciesOptionsFieldWidget`) called with their confirmed existing signatures.
- **Next Task for Next Run:** **PR-2.3 slice 3 — `actionButton` renderer (with rest/level-up service extraction) + `levelUpTable` renderer.** First extract the rest/level-up handlers from `character_editor_screen.dart:2598+` into a small reusable service (`planLevelUp`, short/long-rest flows), then build `_ActionButtonFieldWidget` (renders the FieldSchema label as a button invoking that service per `typeConfig.action ∈ level_up|short_rest|long_rest`; `placement` honored where the sheet exposes header slots) and `_LevelUpTableFieldWidget` (rows `{level, description, grants[], choices[]}` — display + per-row editor, gated by `typeConfig.gate`). Keep resolver/rule changes for Phase 2.4/3. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the built-in PC-category type swap to the v3 parity types once all renderers are live.)*

## [11.06.2026 - 06:00] - Last Update Status
- **Current Phase:** Template Editor UI → Parity Renderers (Roadmap Phase 2 — PR-2.3 slice 1, parity field-type renderers on the PC sheet)
- **Last Completed Task:** **PR-2.3 (slice 1) — v3 parity field-type renderers wired into the PC-sheet `FieldWidgetFactory`, with `abilityScoreTable` made config-driven.** The PR-1.2 v3 `FieldType` values were inert (they fell through to the `_TextFieldWidget` default and would have rendered structured data as raw text). The factory now dispatches the **parity** types to their v2 ancestor's renderer verbatim — the value wire-shapes are byte-identical (roadmap §1.4), so an unconverted card renders pixel-unchanged and the eventual built-in type swap needs **zero** card-value migration for these. Changes (all in **`presentation/widgets/field_widgets/field_widget_factory.dart`**):
  - **`abilityScoreTable` → parameterized `_StatBlockFieldWidget`.** Extracted `_AbilityColumn` + three top-level resolvers: `_resolveAbilityColumns` (reads `typeConfig.columns: [{key,label}]`, falls back to the six SRD scores `_defaultAbilityColumns` — **uppercase keys `STR/DEX/…` to match the existing `{"STR":10,…}` value wire verbatim**), `_abilityModifierBase` (default 10), `_abilityModifierStep` (default 2, 0-guarded against div-by-zero). The widget state now holds a live `_columns` list instead of a `static const _keys`; `initState` seeds controllers per column; **`didUpdateWidget` reconciles the controller map when `typeConfig` changes** (editor live-edit can add/remove/rename columns without a remount — disposes dropped columns, seeds new ones). The modifier uses **truncating** integer division `(score - base) ~/ step` — reproduces today's hardcoded `(score-10)/2` exactly for the default grid (pixel parity; the doc's `floor()` wording is deferred to the Phase-3 resolver to avoid changing rendered modifiers for odd sub-base scores). Legacy `statBlock` fields (no `typeConfig`) hit the same fallback and render identically.
  - **Pure aliases (no parametric change needed this slice):** `combatStatsTable` → `_CombatStatsFieldWidget` (passing the same `levelOverride`/`acOverride`/`armorNotes` as `combatStats`, so the PC's derived level/AC overrides + armor-notes banner survive); `checkboxPouch` → `_SlotFieldWidget` (identical `{count, states[]}` wire); `pouchMatrix` → `_SpellSlotGridFieldWidget` (identical `{max{},remaining{}}` wire; renderer already derives its rows from `max.keys`, so `rowKeys` config isn't needed for parity rendering); `skillTree` → `_ProficiencyTableFieldWidget` (identical `{name,ability,proficient,expertise,misc}` rows); `levelMatrix` → `_SpellSlotProgressionFieldWidget` (the `Map<level,Map<key,int>>` rename target).
  - **Scope discipline:** renderer/factory layer only — **no resolver/runtime change** (Phase 2.4/3) and **no built-in asset regeneration** (the built-in still ships v2 types until the asset is re-exported in a Flutter env, so these arms are exercised only by editor-minted v3 copies for now). The genuinely-**new** types with no v2 ancestor (`intPouch`, `recordList` + presets, `levelUpTable`, `actionButton`) are intentionally **not** added here — they need real new renderers (next slice) and no built-in field uses them yet, so they stay on the existing text fallback rather than rendering as half-built widgets.
  - **Verification:** no Flutter toolchain in this env — verified statically: `{}`/`[]` balance exact; `()` delta unchanged from the committed file (the 2-paren skew is pre-existing comment/string text, not from the edit); all six new switch arms present; the four aliased widget constructors (`_CombatStatsFieldWidget`, `_SlotFieldWidget`, `_SpellSlotGridFieldWidget`, `_ProficiencyTableFieldWidget`, `_SpellSlotProgressionFieldWidget`) called with their confirmed existing signatures (`entityFields`/override params matched against the v2 arms); the parity types are non-list structured fields so the `isList` early-return is untouched.
- **Next Task for Next Run:** **PR-2.3 slice 2 — new-type renderers (`intPouch`, `actionButton`) + `recordList` preset routing.** Build `_IntPouchFieldWidget` (`{current,max}` "23/40" with a tappable +/- and a manual max field, the `maxSource: manual` default) and `_ActionButtonFieldWidget` (renders the FieldSchema label as a button invoking the existing `planLevelUp`/rest flows — extract the rest/level-up handlers from `character_editor_screen.dart:2598+` into a small service first), and route `recordList` to the bespoke preset renderers (`spell-effects`→`RangedSenseListFieldWidget` siblings etc.) by `typeConfig.preset`, falling back to a generic typed-column table when no preset. `levelUpTable` renderer (rows {level,description,grants,choices}) can follow. Keep resolver/rule changes for Phase 2.4/3. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert; and the built-in PC category type swap to the v3 parity types once these renderers are all live.)*

## [11.06.2026 - 05:00] - Last Update Status
- **Current Phase:** Template Editor UI (Roadmap Phase 2 — PR-2.2b per-type `typeConfig` sub-forms)
- **Last Completed Task:** **PR-2.2b — per-type `typeConfig` sub-forms on the responsive editor.** The PR-2.2a field inspector showed `typeConfig` as read-only JSON; it now mounts a live, write-through configuration form for every parametric field type, wired into the same draft/dirty/validation/save pipeline. Changes:
  - **`application/providers/template_editor_provider.dart`** — added the `typeConfig` layer to `TemplateEditorNotifier`. New `updateFieldTypeConfig(categoryId, fieldId, Map)` mutator rebuilds the selected category's field via `copyWith(typeConfig:)`, marks `isDirty`, recomputes validation (empty map → clears to `null`). `addField` now seeds a **valid default `typeConfig`** for parametric types via the new top-level `defaultTypeConfig(FieldType)` (so a freshly added recordList/pouch/abilityScore field is immediately save-valid and its form opens on sensible values — the built-in §2.3 shapes). New `_validateTypeConfig` merged into `_validateAll` (now category ⊕ field ⊕ typeConfig): per-type completeness — abilityScore/recordList column lists (≥1 column, non-empty + unique keys, valid record kind), combatStats visibleKeys (non-empty, canonical), pouch `maxSource`/`countSource` (valid kind; fixed→number, formula→expr, levelTable→≥1 entry), pouchMatrix rowKeys, skillTree tiers, levelUpTable gate, actionButton action. **Crucially it `continue`s on absent config** so legacy v2-typed fields carried over from a copied built-in (no `typeConfig` until the PR-2.3 renderer swap) and non-parametric fields are never false-blocked — only editor-minted v3 fields (which always seed a non-null default) are checked. New exported vocabularies shared with the forms: `combatStatsCanonicalKeys`, `pouchSourceKinds`, `recordListColumnKinds`, `actionButtonActions`, `levelUpTableGates`, `skillTreeTiers`.
  - **`screens/templates/widgets/type_config_forms/`** (new dir, 9 files) — `tc_shared.dart` (shared `TcSection`/`TcLabel`/`TcTextField`/`TcListRow`/`TcAddButton` styled to match the inspector, plus the self-contained **`TcSourceEditor`** reused by all three pouch types: kind dropdown → fixed-value / formula-expr / level-table row editor / manual, owns its own controllers seeded once and emits a freshly-built source map). `type_config_form.dart` — the **dispatcher** (`TypeConfigForm` + static `hasFormFor`) routing each `FieldType` (incl. legacy aliases `statBlock`/`combatStats`/`slot`/`proficiencyTable`/`spellSlotGrid` → their v3 form so a copied built-in stays editable) to: `ability_score_table_form` (column key/label list + base/step + publishAspects), `combat_stats_form` (canonical-key checklist), `pouch_config_form` (intPouch maxSource / checkboxPouch countSource+style / pouchMatrix rowKeys+prefix+maxSource), `skill_tree_form` (ability field/prof aspect/row seed + tier checklist), `record_list_columns_form` (responsive per-column cards: key/label/kind + ref-allowedTypes / enum-options extras + optional preset), `level_up_table_form` (gate), `action_button_form` (action + placement). Each form is `ConsumerStatefulWidget`, seeds controllers once in `initState`, and writes the **whole** config through `updateFieldTypeConfig` on every change.
  - **`screens/templates/widgets/template_field_inspector.dart`** — `_FieldEditForm` replaced the read-only `typeConfig` JSON + "forms arrive next" note with `if (TypeConfigForm.hasFormFor(type)) → "Type configuration" heading + TypeConfigForm(key: tc-<fieldId>)`, falling back to the read-only `_JsonSection` only for the (now rare) non-form configs. Same form mounts on the phone `template_field_edit_page` (`fieldOnly: true`) — full-screen keyboard room — so all three responsive surfaces share it.
  - **Scope discipline:** `typeConfig` forms only. The rule-attachment editor (trigger + kind + params + choice forms) remains **PR-3.5a** — `rules` is still shown read-only beneath the form; no rule UI scaffolded here. Field **type** stays fixed after creation (set once via the picker). `levelMatrix`/`levelTable`/`levelTextTable`/`crCalculator` carry no parametric form (data-only) — dispatcher returns `SizedBox.shrink`.
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren/bracket balance OK across all 11 touched files (one false-positive resolved — apostrophe in a `"Owning class's level"` literal); relative import depths confirmed (`type_config_forms/*` → `../../../../../application|domain`, `../../../../theme`); `DropdownButtonFormField` uses `initialValue:` (matches codebase API per PR-2.2a); dropdowns self-manage display via internal `FormFieldState` so `setState` only toggles conditional sub-widgets (no stale-value risk); every `DmToolColors` member used (`featureCardBg/Border/Accent`, `sidebarLabelSecondary`, `tabActiveText`, `dangerBtnBg`, `htmlCodeBg`) confirmed present; `FieldSchema.copyWith(typeConfig:)` valid; legacy-field guard prevents copied-built-in false-blocks.
- **Next Task for Next Run:** **PR-2.3 — parity field-type renderers + built-in type swap (roadmap PR-T6 slice 1).** Begin parameterizing the v2 renderers behind the new types so the field *list/inspector* preview and (next) the PC sheet read the v3 `typeConfig`: rename/wrap `statBlock`→abilityScoreTable (config-driven columns/base/step), `slot`→checkboxPouch, `spellSlotGrid`→pouchMatrix, `proficiencyTable`→skillTree, with old `FieldType`s still parsing as aliases. Keep it to the renderer/field-widget-factory layer (no resolver/runtime changes — that's Phase 3). *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [11.06.2026 - 04:00] - Last Update Status
- **Current Phase:** Template Editor UI (Roadmap Phase 2 — PR-2.2a live Field CRUD core)
- **Last Completed Task:** **PR-2.2a — live Field CRUD on the responsive editor, core attributes + type picker.** The PR-1.5 read-only field list/inspector are now a full add/edit/reorder/delete surface on editable template copies, wired into the existing draft/dirty/validation/save pipeline (mirrors PR-2.1 category CRUD). Per-type `typeConfig` sub-forms are the next slice (PR-2.2b) — `typeConfig`/`rules` stay read-only here. Changes:
  - **`application/providers/template_editor_provider.dart`** — added the field mutator layer to `TemplateEditorNotifier`: `addField` (mint `fld-<uuid>`, seed a unique in-category `snake_case` key + humanized label from the `FieldType`, `orderIndex` after the last field, selects the new field), `updateFieldMeta` (label/fieldKey/required/isList/hasEquip/visibility/groupId/gridColumnSpan/placeholder/helpText — any subset; `_unset` sentinel so `groupId` can be *cleared*), `removeField` (hard delete — fields have no archive flag — clearing the selection if it was the removed one), and `reorderFields` (Flutter `ReorderableListView` index convention against the `orderIndex`-sorted list, renumbers `orderIndex`). All route through `_commitFields`, which rebuilds the category's `fields` → draft `WorldSchema` via `copyWith`, **marks `isDirty`**, and recomputes validation. Validation unified: `_validateAll = _validateCategories ⊕ _validateFields`; `_commitCategories` switched to `_validateAll` so the single `errors` list (and the Save error summary) reflects both surfaces. New `_validateFields` flags empty labels and empty/malformed/reserved/**duplicate-within-category** field keys. New top-level exports for the editor: `templateFieldKeyPattern` (snake_case grammar, distinct from the hyphen category-slug grammar), `fieldKeyNormalize`, `reservedFieldKeys` (`{id, slug, name, format}`).
  - **`screens/templates/widgets/field_type_picker.dart`** (new) — responsive **field-type picker**: **`showModalBottomSheet` (FractionallySizedBox 0.85) on `isTouchPlatform`, `showDialog` (640×620) on pointer/desktop** (roadmap §1.5). Sectioned grid (Basic / Media / Relations & records / Resources / Tables / Actions & tools) over the **creatable** v3 type set (`kCreatableFieldTypeSections`) — legacy v2 aliases (`statBlock`, `slot`, `spellSlotGrid`, `proficiencyTable`, the bespoke effect lists, …) are intentionally excluded (they exist only to parse existing content). Each card pulls label/icon/one-line summary from the shared `field_type_meta.dart`; rule-capable types carry a "rules" badge. Two-up on ≥440px, single column on narrow phones.
  - **`screens/templates/widgets/template_field_list_pane.dart`** (rewritten) — read-only path unchanged (`ListView` + selection + rule badge). Editable path now uses **`ReorderableListView.builder`** with explicit `ReorderableDragStartListener` handles (taps still select), a per-tile **Delete** `PopupMenuButton` (confirm dialog noting cards keep stored values under the key), and a working **"+ Add field"** row → `showFieldTypePicker` → `addField`. On touch surfaces the add flow re-fires `onFieldTap` with the freshly-selected field so the phone flow drills straight into its edit page. Works across desktop 3-pane, tablet master-drill, and phone stacked surfaces (all share the one provider).
  - **`screens/templates/widgets/template_field_inspector.dart`** (rewritten) — built-in read-only path preserved (`_FieldDetail`). Editable copies get a new **`_FieldEditForm`** (`ConsumerStatefulWidget`, **keyed by `fieldId`** so controllers reset on selection change) writing straight through to `updateFieldMeta` on every change: Label + Key text fields (live snake_case normalization + inline duplicate/format/reserved feedback), Required / List / Equippable (relation-only) switches, Visibility dropdown, Group dropdown (from `category.fieldGroups`, "Ungrouped" clears it), a 1–4 Column-span stepper, Placeholder + Help text. Type is fixed after creation (shown in the header with its rule-capable bolt). `typeConfig` (raw JSON) + `rules` shown read-only beneath with a "per-type forms arrive next" note. The phone `template_field_edit_page` (`fieldOnly: true`) mounts this same form — full-screen keyboard room.
  - **Scope discipline:** field core-attribute CRUD only. Per-type `typeConfig` forms (`type_config_forms/*`: ability-score-table, combat-stats, pouch-config, skill-tree, record-list-columns, level-up-table, action-button) are PR-2.2b; the rule-attachment editor is PR-3.5a — neither scaffolded here (no dead code). Field **type** is not editable after creation in 2.2a (value-shape migration risk); set once via the picker.
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren/bracket balance OK across all 4 files; `DropdownButtonFormField` uses `initialValue:` (matches the codebase's current Flutter API — confirmed against `field_widget_factory.dart`, `entity_sidebar.dart`, marketplace/listing dialogs); `FieldSchema.copyWith` fields all exist; `EntityCategorySchema.copyWith(fields:)` + `FieldGroup.{groupId,name,orderIndex}` confirmed; `isTouchPlatform` import path matches `category_edit_sheet.dart`; every referenced `DmToolColors` member (`featureCardAccent/Bg/Border`, `sidebarLabelSecondary/Divider/FilterBg`, `tabActiveText/Bg`, `dangerBtnBg`, `htmlCodeBg`) confirmed present; editor screen + phone pages instantiate the panes with unchanged constructors.
- **Next Task for Next Run:** **PR-2.2b — per-type `typeConfig` sub-forms.** Build `screens/templates/widgets/type_config_forms/*.dart` (one form per parametric type — `ability_score_table_form` [columns + modifierBase/Step + publishAspects], `combat_stats_form` [visibleKeys checklist], `pouch_config_form` [shared maxSource/countSource editor for intPouch/checkboxPouch/pouchMatrix], `skill_tree_form`, `record_list_columns_form`, `level_up_table_form`, `action_button_form`) per the-template-system §2.3 wire shapes, mounted in `_FieldEditForm` in place of the read-only `typeConfig` JSON (row-based configs reuse the `_StructuredListShell` row pattern). Add a `updateFieldTypeConfig(categoryId, fieldId, Map)` mutator + `typeConfig` completeness validation merged into `_validateAll`. Keep the rule editor for PR-3.5a. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [11.06.2026 - 03:00] - Last Update Status
- **Current Phase:** Template Editor UI (Roadmap Phase 2 — PR-2.1 live Category CRUD)
- **Last Completed Task:** **PR-2.1 — live Category CRUD on the responsive editor shell.** The PR-1.5 read-only category pane is now a full create/edit/reorder/archive surface on editable template copies, wired into the existing draft/dirty/validation/save pipeline. Changes:
  - **`application/providers/template_editor_provider.dart`** — added the category mutator layer to `TemplateEditorNotifier`: `addCategory` (mint `cat-<uuid>` id, slug-normalized, selects the new category), `updateCategoryMeta` (name/slug/icon/color, any subset), `toggleCategoryArchived` (soft-delete — no hard category delete in 2.1), and `reorderCategories` (Flutter `ReorderableListView` index convention, renumbers `orderIndex`). All route through `_commitCategories`, which rebuilds the draft `WorldSchema` via `copyWith`, **marks `isDirty`**, and recomputes blocking validation via `_validateCategories` (empty name, empty/malformed/reserved/**duplicate** slug). New top-level helpers exported for the editor + import layer: `templateSlugPattern` (slug grammar), `categorySlugify`, `reservedCategorySlugs`. The Save flow (PR-1.5) already refuses to persist while `errors` is non-empty, so this plugs straight in.
  - **`screens/templates/widgets/category_edit_sheet.dart`** (new) — responsive add/edit form: **`showModalBottomSheet` on `isTouchPlatform`, `showDialog` on pointer/desktop** (roadmap §1.5). Name field (auto-derives slug in create mode until the slug is hand-edited), slug field with live normalization + inline duplicate/format/reserved feedback, an 18-swatch color picker, and a curated icon picker whose names are all resolvable by the shared `_iconFromName` map (so picks render on the world map etc.). Returns a `CategoryEditResult`; the notifier re-validates on commit (defense in depth).
  - **`screens/templates/widgets/template_category_pane.dart`** (rewritten) — read-only path unchanged (`ListView` + chevron). Editable path now uses **`ReorderableListView.builder`** with explicit `ReorderableDragStartListener` handles (so taps still select/drill), a per-tile **Edit / Archive** `PopupMenuButton`, archived tiles shown italic + "· archived", and a working **"+ Add category"** row → `showCategoryEditSheet` → `addCategory`. Works across desktop 3-pane, tablet master-drill, and phone stacked surfaces (all share the one provider).
  - **`application/services/package_import_service.dart`** — **slug-first import-matching hardening** (roadmap §Phase 2.1, prerequisite to shipping rename). Package→world category matching now keys on the stable `slug` first (`worldCatBySlug`), falling back to `name` only for legacy packs whose slug drifted; `pkgCat` resolved via `pkgCatBySlug`. This guarantees a renamed category still receives its pack rows (the old name-only match would have orphaned them). Removed the now-dead `pkgCatByName` map.
  - **Scope discipline:** category CRUD only. Field CRUD (type picker + `typeConfig` forms) is PR-2.2; the rule-attachment editor is PR-3.5a — neither scaffolded here. No hard category delete (archive is the soft-delete per the editor model).
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren/bracket balance OK across all 4 files; no dangling refs to the removed `pkgCatByName`; `worldCat` correctly switched to `var` for the fallback reassignment; `EntityCategorySchema.copyWith` fields (`name/slug/icon/color/isArchived/orderIndex/updatedAt`) all exist; records (`(String, IconData)`) already used elsewhere in the codebase; every `DmToolColors` member used (`featureCardAccent/Bg/Border`, `sidebarFilterBg/Divider/LabelSecondary`, `tabActiveText`) confirmed present; import ordering normalized.
- **Next Task for Next Run:** **PR-2.2 — live Field CRUD + `typeConfig` forms.** Make `TemplateFieldListPane` editable on copies: add/remove/reorder fields within the selected category, a **field-type picker** (bottom-sheet on touch / dialog on desktop, driven by `field_type_meta.dart`'s 44-type metadata), and per-type **`typeConfig` forms** + core field attributes (label, fieldKey, required, visibility, group, gridColumnSpan) edited through `TemplateFieldInspector`. Add field mutators to `TemplateEditorNotifier` (mirror the category ones: rebuild draft → mark dirty → validate **field-key uniqueness within the category** + reserved keys, merging into `state.errors` alongside the category errors). Keep the rule-attachment editor for PR-3.5a. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [11.06.2026 - 02:00] - Last Update Status
- **Current Phase:** Template Editor UI (Roadmap Phase 1 — PR-1.5 responsive editor shell, read-only landing)
- **Last Completed Task:** **PR-1.5 — responsive Template Editor shell landed read-only.** Replaced the old single-layout read-only inspector (`hub/template_editor.dart`, now **deleted**) with the full responsive shell from roadmap §1.5 — the layout contract every Phase 2 CRUD component plugs into. New files:
  - **`application/providers/template_editor_provider.dart`** (new) — `TemplateEditorState` (immutable: `schema`, `isBuiltin`, `selectedCategoryId`/`selectedFieldId`, `isDirty`, `errors`, `isSaving`; `canEdit`, `selectedCategory`/`selectedField` helpers; sentinel-based `copyWith` so nullable selections can be explicitly cleared). `TemplateEditorNotifier extends StateNotifier` (matches the codebase's `save_state_provider` idiom) with `load()` (selects first category so the field list is never empty on open; idempotent re-load preserves place), `selectCategory`/`selectField`/`clearFieldSelection`, and a **fully-wired `save()`** (validate → `TemplateRepository.save` recompute-hash → `invalidate(templateLibraryProvider)`) that returns `TemplateSaveResult.noop` in PR-1.5 because nothing flips `dirty` yet — Phase 2 mutators only need to mark the draft dirty. `autoDispose` so each open starts fresh.
  - **`screens/templates/template_editor_screen.dart`** (rewritten) — `ConsumerStatefulWidget`; loads the routed schema into the controller via a post-frame callback (built-in detected by `schemaId == builtinDnd5eV3SchemaId`); `getScreenType` dispatch → **desktop ≥1200 3-pane** (`SizedBox(240) [Categories]` │ `VerticalDivider` │ `ResizableSplit([Fields] | [Inspector])`), **tablet 600–1199 2-pane** (`ResizableSplit(master | inspector)` where the master `AnimatedSwitcher`-drills category list → field list in place with a back chevron), **phone <600 stacked** (returns the phone `TemplateCategoriesPage`). App-bar carries the **Save button + dirty dot** (disabled until dirty) on editable copies, **"Copy to edit"** on the built-in; `_copyToEdit()` does a functional `repo.copy` with the library's "(Copy N)" collision suggestion, invalidates the library, and reopens the editor on the now-editable copy.
  - **`screens/templates/widgets/template_category_pane.dart`** (new) — read-only category list driving selection into the provider; color dot + field count; disabled "+ Add category" row shown only on editable copies (wired Phase 2.1); optional `onCategoryTap` for tablet drill / phone push.
  - **`screens/templates/widgets/template_field_list_pane.dart`** (new) — per-category field list (sorted by `orderIndex`); type icon, `Type · list · key` subtitle, required star, **rule-count badge**; optional header (hidden on phone, where the AppBar owns the title) with back chevron for tablet; disabled "+ Add field" on editable copies (wired Phase 2.2).
  - **`screens/templates/widgets/template_field_inspector.dart`** (new) — read-only field detail (Definition / Guidance / Validation sections, pretty-printed `typeConfig` + `rules` JSON, a "rule-free until Phase 3" note when empty) with a category-metadata fallback when no field is selected. `fieldOnly` mode for the phone edit page.
  - **`screens/templates/widgets/field_type_meta.dart`** (new) — presentation metadata (label, icon, one-line summary, `ruleCapable` flag per master-roadmap §2.1) for all 44 `FieldType`s incl. legacy aliases; feeds the list/inspector now and the Phase 2.2 type picker later.
  - **`screens/templates/widgets/template_builtin_banner.dart`** (new) — persistent "Built-in template — make a copy to edit" strip with inline Copy action (roadmap §1.5).
  - **`screens/templates/phone/{template_categories_page,template_fields_page,template_field_edit_page}.dart`** (new) — the stacked phone flow on the root navigator (Categories → Fields → Field edit; system back pops within the editor then leaves it), each a `Scaffold` with its own AppBar; reuse the shared panes/inspector so all three responsive surfaces stay in lockstep via the single provider.
  - **Scope discipline:** read-only landing only — no field/category mutation, no type picker, no typeConfig forms, no rule editor (those are PR-2.1/2.2/3.5a). The Save pipeline + dirty/validation state already exist so Phase 2 plugs straight in. `field_type_picker.dart`, `type_config_forms/*`, `category_edit_sheet.dart`, `rule_attachment_editor.dart` are intentionally **not** scaffolded yet (created in the PR that implements them, to avoid dead code).
  - **Verification:** no Flutter toolchain in this env — verified statically: brace/paren balance OK across all 10 files; all 44 `FieldType` enum values covered in `field_type_meta` (missing → graceful `_fallback`, no compile risk); every referenced `DmToolColors` member (`htmlCodeBg`, `featureCardBg/Border/Accent`, `sidebarFilterBg/Divider/LabelSecondary`, `tabBg`, `tabActiveText`, `dangerBtnBg`) confirmed present; all Material icon names verified; no dangling references to the deleted `hub/template_editor.dart`; router import + `templates_tab._openEditor` path intact.
- **Next Task for Next Run:** **PR-2.1 — live Category CRUD on the responsive shell** (first Phase 2 step). Wire `TemplateCategoryPane`'s reorder handles + "+ Add category" row and a `category_edit_sheet.dart` (name/slug/icon/color — sheet on touch, dialog on desktop) into `TemplateEditorNotifier` mutators (add/rename/archive/reorder) that rebuild the draft `WorldSchema`, mark `isDirty`, and run validation (duplicate slug, empty name, reserved keys) surfaced in `state.errors` + the Save error summary. Include the **slug-first import-matching hardening** (roadmap §Phase 2.1) before rename ships. Keep field CRUD (type picker + typeConfig forms) for PR-2.2. *(Also still pending, Flutter-env only: PR-1.3 closeout — `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [11.06.2026 - 00:30] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.4 Template library + Copy, UI half)
- **Last Completed Task:** **PR-1.4 (UI half) — rebuilt `templates_tab.dart` as the Template library CRUD** on the `packages_tab.dart` pattern. The hub Templates tab is now a working library instead of a read-only browser:
  - **Provider swap.** The tab now watches **`templateLibraryProvider`** (`[builtin asset] + user templates`, built-in first) instead of the old `allTemplatesProvider` (which still feeds world/character creation the v2 schema — untouched, per the dual-stack invariant). `TemplatesTab` is now a `ConsumerStatefulWidget` so it can drive dialogs/snackbars/invalidations.
  - **Per-tile actions, not a shared selection bar.** Built-in and user templates have different action sets, so each tile carries its own actions: **built-in → [View] [Copy]** (read-only); **user copies → [Edit] [Copy] [Rename] [Delete]**. Built-in is detected by `schema.schemaId == builtinDnd5eV3SchemaId`.
  - **Built-in read-only affordances (roadmap §1.5).** Built-in tile shows the dnd5e banner art, a "Read-only" badge next to the name, and a persistent `_CopyToEditBanner` strip — *"Built-in template — make a copy to edit."* — with an inline **Copy** button. Rename/Delete are wired `null` for the built-in (the repository also structurally cannot touch it — it operates only on the `templates` table).
  - **`_copyTemplate()`** mirrors `_copyPackage()`: "(Copy)" suggestion via `_suggestCopyName()` with " (Copy 2/3/…)" collision suffixing against the **whole** library (built-in name included, so a copy never shadows it), a name dialog (autofocus + submit-on-enter), a double collision guard (`existingNames` **and** `TemplateRepository.nameExists`), `repo.copy(source:, newName:)`, `ref.invalidate(templateLibraryProvider)`, and a confirmation snackbar.
  - **`_renameTemplate()` / `_deleteTemplate()`** via `TemplateRepository.rename` / `delete`. Rename validates against *other* templates' names (excludes self, no-ops on unchanged). Delete is confirm-gated with a danger-styled button and a note that already-created worlds/packages keep their embedded copy and are unaffected.
  - **Responsive action layout (`_TileActions`).** A `LayoutBuilder` switches per-tile: when the footer has comfortable room for the primary + every secondary button (`maxWidth >= 110 + count*116`) all actions render inline (tablet/desktop, card capped at `kCardMaxWidth=580`); on narrow phone tiles the primary action stays inline and the secondaries fold into an overflow `PopupMenuButton`. The full responsive *editor shell* is still PR-1.5 — this is the library-tab CRUD surface only.
  - **Verification:** no Flutter toolchain in this env, so verified statically — brace/paren/bracket balance OK; every `DmToolColors` member used (`featureCardAccent`, `dangerBtnBg/Text`, `featureCard*`, `tabActiveText`, `sidebarLabelSecondary`, `br`) confirmed present; all imports used; `builtinDnd5eV3SchemaId`, `templateLibraryProvider`, `templateRepositoryProvider` confirmed exported.
- **Next Task for Next Run:** **PR-1.5 — responsive Template Editor shell (read-only landing).** Replace the current read-only inspector wrapper (`screens/templates/template_editor_screen.dart` → `hub/template_editor.dart`) with the responsive shell from roadmap §1.5: `getScreenType` dispatch — desktop ≥1200 3-pane (`[Categories 240px] | ResizableSplit([Fields] | [Field Inspector])`), tablet 600–1199 2-pane (category→field drill + inspector), phone <600 stacked nested-`Navigator` (`TemplateCategoriesPage → TemplateFieldsPage → TemplateFieldEditPage`). Land it **read-only** (the layout contract Phase 2 components plug into) with the app-bar Save + dirty-dot affordances stubbed and the built-in read-only banner. Scaffold the `template_editor_provider.dart` draft/dirty/validation Notifier (no mutation wiring yet) and the `screens/templates/widgets/` component files as the digestible unit. *(Also still pending, Flutter-env only: PR-1.3 closeout — run `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [10.06.2026 - 23:30] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.4 Template library + Copy, persistence half)
- **Last Completed Task:** **PR-1.4 (persistence half) — `templates` Drift table + `TemplateRepository` + library provider.** Landed the whole storage/repository spine for the hub-level Template library, mirroring the `personal_packages`/`packages` stack so the editor (PR-1.5 / Phase 2) and the rebuilt `templates_tab.dart` plug straight in:
  - **`templates_table.dart`** (new, `lib/data/database/tables/`) — `Templates` Drift table: `id` (PK = `schemaId`), `name`, `dataJson` (full `WorldSchema.toJson()` blob — single source of truth), `originalHash` + `currentHash` (denormalised drift-detection hashes), `createdAt`/`updatedAt`. The built-in template is **deliberately not a row** (it is the read-only `BuiltinTemplateLoader` asset); the library list is `[builtin asset] + table rows`.
  - **`templates_dao.dart`** (new, `lib/data/database/daos/`) — `TemplatesDao` (`@DriftAccessor`): `getAll`/`watchAll` (ordered `updated_at DESC`), `getById`, `getByName` (copy-collision check), `upsert` (insertOnConflictUpdate), `rename` (fast label update), `deleteById`. Thin CRUD only — all schema parsing/hashing lives in the repo.
  - **`template_repository.dart`** (new, `lib/domain/repositories/`) — `TemplateRepository` interface: `listUserTemplates`, `load`, `save`, `copy`, `rename`, `delete`, `nameExists`. Knows nothing about the built-in, so it can never rename/delete/overwrite it.
  - **`template_repository_impl.dart`** (new, `lib/data/repositories/`) — Drift-backed impl. `save()` recomputes `computeWorldSchemaContentHash`, backfills + preserves `originalHash`, preserves `createdAt` on update / stamps on insert, keeps the blob and the denormalised columns in lockstep. **`copy()` = fresh `schemaId` (`tmpl-<uuid>`), preserved `originalHash`** (roadmap §1.4 — lineage survives drift detection) — seeds lineage from the source's current hash if it had none. `_tryParse` is defensive (corrupt blob → skipped; row id/name win over a disagreeing blob).
  - **`app_database.dart`** — registered `Templates` + `TemplatesDao` (imports, `tables:`, `daos:`); **`schemaVersion 12 → 13`** with a **real stepwise `onUpgrade`** (`if (from < 13) createTable(templates)` + index) replacing the old blind `createAll()` (which would have thrown "table already exists" on the now-live 12→13 path for existing installs); `onCreate` also runs the new `idx_templates_updated` index, added as the `_templatesIndex` constant.
  - **`template_provider.dart`** — added `templateRepositoryProvider` (DI over `appDatabaseProvider`, mirrors `packageRepositoryProvider`) and **`templateLibraryProvider`** (`FutureProvider<List<WorldSchema>>` = `[builtin asset] + listUserTemplates()`, built-in first). **`allTemplatesProvider` left untouched** (still hands the **v2** schema to world/character creation) — per the dual-stack invariant (roadmap §1.4) new worlds must keep resolving on the frozen v2 engine until the new resolver lands; the v3 library is a separate surface and the authority flip is Phase 3.11. Purely additive — no existing call site changes behavior.
  - **Scope discipline:** persistence/repository/provider only, **no UI** (the digestible unit per the prior plan). The `templates_tab.dart` rebuild (Load / Copy / Rename / Delete on the `packages_tab.dart` pattern) is the next step.
- **Next Task for Next Run:** **PR-1.4 (UI half) — rebuild `templates_tab.dart` on the `packages_tab.dart` pattern.** Wire the new `templateLibraryProvider` into the hub Templates tab: list `[built-in (read-only: View + Copy)] + user copies (Edit/Rename/Delete)`; implement `_copyTemplate()` mirroring `_copyPackage()` (`packages_tab.dart:437` — "(Copy)" suggestion, collision suffix via `TemplateRepository.nameExists`, name dialog, `ref.invalidate(templateLibraryProvider)`); rename/delete via `TemplateRepository.rename`/`delete`. Built-in tile gets read-only affordances + the "make a copy to edit" banner (roadmap §1.5). Keep it to the library tab CRUD — the responsive `TemplateEditorScreen` shell is PR-1.5. *(Also still pending, Flutter-env only: PR-1.3 closeout — run `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [10.06.2026 - 22:15] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.3 Built-in template → JSON asset, RULE RESET)
- **Last Completed Task:** **PR-1.3 (infrastructure half) — Built-in template → JSON asset scaffold.** Landed everything for the v3 asset pipeline that is implementable without a Flutter toolchain (the asset *bytes* + the runtime hash-assert are confirmed in a Flutter env — see Next Task):
  - **`builtin_dnd5e_template_v3.dart`** (new) — `generateBuiltinDnd5eTemplateV3()`: the **single source of truth** consumed by both the exporter and the loader's assert, so the on-disk asset and the in-code generator can never silently diverge. It evolves `generateBuiltinDnd5eV2Schema().schema` *in place* via `copyWith`: `formatVersion → 3`, `schemaId/originalHash → builtin-dnd5e-default-**v3**` (distinct lineage so v2 and v3 worlds coexist under per-world dual-stack authority, roadmap §1.4), `version → 3.0.0`, `name → "D&D 5e (Default)"`. **`seedRows` lifted** out of the generator's typed side-channel (`BuiltinDnd5eV2Build.seedRows`) and **embedded** into the template via `liftSeedRows()` (Tier-0 lookup catalogs only — empty Tier-1/2 entries dropped to keep the asset lean). **Deterministic timestamp** (`builtinDnd5eTemplateTimestamp`) stamped so re-exports are byte-identical (idempotent; hash-excluded `createdAt/updatedAt` so drift detection is unaffected). **RULE RESET:** no field carries `rules` — left `null` (omitted via `includeIfNull:false`) so the *only* v2→v3 hash delta is the embedded `seedRows`; "zero rule-assigned fields" is the contract, the empty-vs-absent distinction is cosmetic. Old `FieldType`s kept (type swap is PR-2.3).
  - **`tool/export_builtin_template.dart`** (new) — one-shot CLI exporter (`dart run tool/export_builtin_template.dart`). Serializes the v3 template to `assets/templates/dnd5e_srd.template.json`, **canonicalized** (recursively key-sorted, matching `world_schema_hash.dart`) and **pretty-printed** (2-space) so JIT wave edits diff line-by-line (roadmap §3.1). Enforces the **rule-free invariant** (aborts with exit 2 if any field has `rules`); prints `categories`/`seedRows`/`formatVersion`/`contentHash` for cross-checking against the loader assert.
  - **`builtin_template_loader.dart`** (new, `lib/data/templates/`) — `BuiltinTemplateLoader` singleton: cached async `load()` (single in-flight load, `cachedOrNull` sync accessor, `@visibleForTesting resetCache`). On asset present → parse + **debug-only `assert`** that `computeWorldSchemaContentHash(asset) == computeWorldSchemaContentHash(generator)` (loud `FlutterError` naming the exporter if stale). On asset absent → **graceful fallback** to the generator (loud `debugPrint` in debug) so the app works in a fresh checkout before the exporter is run.
  - **`pubspec.yaml`** — registered `assets/templates/` (with an explanatory comment that the JSON is generated, not hand-authored). `assets/templates/.gitkeep` committed so the registered dir always exists.
  - **Inertness / scope discipline:** per the prior plan, **no call sites were swapped** — all 20+ `generateBuiltinDnd5eV2Schema()` consumers (template provider, bootstraps, world/package repos, import dialogs) are untouched; the loader ships as scaffold only. New code is purely additive; nothing existing changed behavior.
- **Next Task for Next Run:** **PR-1.3 (closeout, Flutter-env) → then PR-1.4 (Template library + Copy).** Two parts:
  1. *(Flutter env, one-time)* Run `dart run tool/export_builtin_template.dart`, **commit the generated `assets/templates/dnd5e_srd.template.json`**, boot the app in debug and confirm `BuiltinTemplateLoader.load()` passes the hash-equality assert (asset hash == generator hash). This is the only PR-1.3 step that needs the toolchain.
  2. *(Implementable here)* Begin **PR-1.4**: add the `templates` Drift table (one JSON blob per row: `id, name, dataJson, originalHash, currentHash, createdAt, updatedAt`) + migration, a `TemplateRepository` (list/load/save/copy/delete/rename) mirroring `PackageRepository`, and a `templateListProvider` where `allTemplatesProvider` becomes `[BuiltinTemplateLoader] + tableRows`. Copy semantics: **fresh `schemaId`, preserved `originalHash`** (lineage survives for drift detection). UI rebuild of `templates_tab.dart` follows in the same PR. Start with the table + repository (no UI) as the digestible unit.

## Todo List
- [x] Clean up legacy rule panels from the old UI *(PR-1.1 — done)*
- [ ] Implement the core Template System infrastructure *(PR-1.2 model v3 inert ✅ → PR-1.3 builtin JSON export — scaffold ✅, asset/assert pending Flutter env → PR-1.4 template library/copy: table + DAO + repository + `templateLibraryProvider` + `templates_tab.dart` UI ✅)*
- [ ] **Develop the mobile and desktop responsive Template Editor UI for dynamic template modification** *(PR-1.5 responsive shell + editor provider — read-only landing ✅; live category CRUD PR-2.1 ✅; field CRUD core — mutators + type picker + editable list/inspector PR-2.2a ✅; per-type typeConfig forms PR-2.2b ✅; parity PC-sheet renderers PR-2.3 slice 1 — abilityScoreTable/combatStatsTable/checkboxPouch/pouchMatrix/skillTree/levelMatrix aliased ✅; slice 2 — intPouch renderer + recordList preset routing (+ generic typed-column table) ✅; slice 3a — levelUpTable renderer ✅; slice 3b — actionButton renderer w/ onAction seam ✅; built-in PC type swap: slice A parity renames ✅, slice B part 1 — death-save/inspiration int→checkboxPouch + `legacy_content_converter` pip shim ✅, part 2 (actionButton header fields + retire `_renderRestActions`) ✅, part 3 (static-field preservation checklist) ✅; checkboxPouch `style:'pips'` fixed-count renderer ✅ — PR-2.3 visual parity complete; PR-2.4 TemplateRuleResolver shadow: slice 1 — resolver skeleton + `modify_stat`/`fixed` kind + dev harness ✅; slice 2 — `AspectContext` (§4.3) + `field` value source ✅; slice 3 — `formula` value source + `formula_evaluator.dart` (§4.3 grammar) ✅ → `modify_stat` complete; slice 4 — `note` + `check_clauses` text kinds ✅; slice 5 — `grant_refs` + `grant_proficiency` collection kinds ✅; slice 6 — `choose` pending-choice emission ✅; slice 7 — `set_pouch_max` (pouch max from levelTable/levelMatrix/formula, multiclass aggregation) ✅; slice 8 — `refill_pouch`/`empty_pouch` imperative `on_button` pair via `applyButton` ✅; slice 9 — `grant_pouch` (data-driven class-resource pouches, §2.4) ✅ → **ALL rule kinds implemented**; remaining: `choose` perPick re-fold (PR-2.5), then call-site flip/debug panel + rule editor PR-3.5a)*
- [ ] Write the Built-in SRD Template as JSON (without advanced rule fields initially, preserving static text/descriptions) *(PR-1.3, RULE RESET: rule-free; asset materialization pending `dart run tool/export_builtin_template.dart`)*
- [ ] Migrate the Built-in DnD Package (Cards/Entities), enrich descriptions using Markdown, and evolve the template dynamically with rules (Just-In-Time) *(Phase 3 waves)*
- [ ] Clean up redundant template fields, run **UI responsive layout tests**, and conduct final integration testing *(Phase 4)*

## Notes / Constraints
- Working on branch **`new-rules`**; commit every change here.
- `flutter`/`dart` toolchain is **not installed** in this remote environment, so `flutter analyze` cannot be executed here and the exporter cannot be run — changes are verified by static grep sweeps, brace/paren/bracket balance, and dependency-graph analysis. The analyze gate + the exporter run + the loader hash-assert must be confirmed in a Flutter-capable environment before release.
- **PR-1.3 asset is intentionally NOT committed yet** because it is a generated artifact (`generateBuiltinDnd5eTemplateV3().toJson()`); fabricating it by hand would risk a hash mismatch against the generator and defeat the assert. The exporter + loader + deterministic builder are all in place so a single `dart run` produces it reproducibly. The loader's graceful fallback keeps the app functional in the interim.
- Per the master roadmap, this roadmap wins over the two detail docs (notably: rule text goes into the card `description`, not a `rules_text` side-field; JIT evolution replaces a bulk ruleset drop).
- Vault is intentionally **not** updated until the whole roadmap + tests are finished (per run instructions).
