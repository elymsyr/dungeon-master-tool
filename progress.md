# Project Progress Report (progress.md)

## [11.06.2026 - 00:30] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.4 Template library + Copy, UI half)
- **Last Completed Task:** **PR-1.4 (UI half) — rebuilt `templates_tab.dart` as the Template library CRUD** on the `packages_tab.dart` pattern. The hub Templates tab is now a working library instead of a read-only browser:
  - **Provider swap.** The tab now watches **`templateLibraryProvider`** (`[builtin asset] + user templates`, built-in first) instead of the old `allTemplatesProvider` (which still feeds world/character creation the v2 schema — untouched, per the dual-stack invariant). `TemplatesTab` is now a `ConsumerStatefulWidget` so it can drive dialogs/snackbars/invalidations.
  - **Per-tile actions, not a shared selection bar.** Built-in and user templates have different action sets, so each tile carries its own actions: **built-in → [View] [Copy]** (read-only); **user copies → [Edit] [Copy] [Rename] [Delete]**. Built-in is detected by `schema.schemaId == builtinDnd5eV3SchemaId`.
  - **Built-in read-only affordances (roadmap §1.5).** Built-in tile shows the dnd5e banner art, a "Read-only" badge next to the name, and a persistent `_CopyToEditBanner` strip — *"Built-in template — make a copy to edit."* — with an inline **Copy** button. Rename/Delete are wired `null` for the built-in (the repository also structurally cannot touch it — it operates only on the `templates` table).
  - **`_copyTemplate()`** mirrors `_copyPackage()`: "(Copy)" suggestion via `_suggestCopyName()` with " (Copy 2/3/…)" collision suffixing against the **whole** library (built-in name included, so a copy never shadows it), a name dialog (autofocus + submit-on-enter), a double collision guard (`existingNames` **and** `TemplateRepository.nameExists`), `repo.copy(source:, newName:)`, `ref.invalidate(templateLibraryProvider)`, and a confirmation snackbar.
  - **`_renameTemplate()` / `_deleteTemplate()`** via `TemplateRepository.rename` / `delete`. Rename validates against *other* templates' names (excludes self, no-ops on unchanged). Delete is confirm-gated with a danger-styled button and a note that already-created worlds/packages keep their embedded copy and are unaffected.
  - **Responsive action layout (`_TileActions`).** A `LayoutBuilder` switches per-tile: when the footer has comfortable room for the primary + every secondary button (`maxWidth >= 110 + count*116`) all actions render inline (tablet/desktop, card capped at `kCardMaxWidth=580`); on narrow phone tiles the primary action stays inline and the secondaries fold into an overflow `PopupMenuButton`. The full responsive *editor shell* is still PR-1.5 — this is the library-tab CRUD surface only.
  - **Verification:** no Flutter toolchain in this env, so verified statically — brace/paren/bracket balance OK; every `DmToolColors` member used (`featureCardAccent`, `dangerBtnBg/Text`, `featureCard*`, `tabActiveText`, `sidebarLabelSecondary`, `br`) confirmed present; all imports used; `builtinDnd5eV3SchemaId`, `templateLibraryProvider`, `templateRepositoryProvider` confirmed exported.
- **Next Task for Next Run:** **PR-1.5 — responsive Template Editor shell (read-only landing).** Replace the current read-only inspector wrapper (`screens/templates/template_editor_screen.dart` → `hub/template_editor.dart`) with the responsive shell from roadmap §1.5: `getScreenType` dispatch — desktop ≥1200 3-pane (`[Categories 240px] | ResizableSplit([Fields] | [Field Inspector])`), tablet 600–1199 2-pane (category→field drill + inspector), phone <600 stacked nested-`Navigator` (`TemplateCategoriesPage → TemplateFieldsPage → TemplateFieldEditPage`). Land it **read-only** (the layout contract Phase 2 components plug into) with the app-bar Save + dirty-dot affordances stubbed and the built-in read-only banner. Scaffold the `template_editor_provider.dart` draft/dirty/validation Notifier (no mutation wiring yet) and the `screens/templates/widgets/` component files as the digestible unit. *(Also still pending, Flutter-env only: PR-1.3 closeout — run `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

### Earlier history
## [10.06.2026 - 23:30] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.4 Template library + Copy, persistence half)
- **Last Completed Task:** **PR-1.4 (persistence half) — `templates` Drift table + `TemplateRepository` + library provider.** Landed the whole storage/repository spine for the hub-level Template library, mirroring the `personal_packages`/`packages` stack so the editor (PR-1.5 / Phase 2) and the rebuilt `templates_tab.dart` plug straight in:
  - **`templates_table.dart`** (new, `lib/data/database/tables/`) — `Templates` Drift table: `id` (PK = `schemaId`), `name`, `dataJson` (full `WorldSchema.toJson()` blob — single source of truth), `originalHash` + `currentHash` (denormalised drift-detection hashes), `createdAt`/`updatedAt`. The built-in template is **deliberately not a row** (it is the read-only `BuiltinTemplateLoader` asset); the library list is `[builtin asset] + table rows`.
  - **`templates_dao.dart`** (new, `lib/data/database/daos/`) — `TemplatesDao` (`@DriftAccessor`): `getAll`/`watchAll` (ordered `updated_at DESC`), `getById`, `getByName` (copy-collision check), `upsert` (insertOnConflictUpdate), `rename` (fast label update), `deleteById`. Thin CRUD only — all schema parsing/hashing lives in the repo.
  - **`template_repository.dart`** (new, `lib/domain/repositories/`) — `TemplateRepository` interface: `listUserTemplates`, `load`, `save`, `copy`, `rename`, `delete`, `nameExists`. Knows nothing about the built-in, so it can never rename/delete/overwrite it.
  - **`template_repository_impl.dart`** (new, `lib/data/repositories/`) — Drift-backed impl. `save()` recomputes `computeWorldSchemaContentHash`, backfills + preserves `originalHash`, preserves `createdAt` on update / stamps on insert, keeps the blob and the denormalised columns in lockstep. **`copy()` = fresh `schemaId` (`tmpl-<uuid>`), preserved `originalHash`** (roadmap §1.4 — lineage survives drift detection) — seeds lineage from the source's current hash if it had none. `_tryParse` is defensive (corrupt blob → skipped; row id/name win over a disagreeing blob).
  - **`app_database.dart`** — registered `Templates` + `TemplatesDao` (imports, `tables:`, `daos:`); **`schemaVersion 12 → 13`** with a **real stepwise `onUpgrade`** (`if (from < 13) createTable(templates)` + index) replacing the old blind `createAll()` (which would have thrown "table already exists" on the now-live 12→13 path for existing installs); `onCreate` also runs the new `idx_templates_updated` index, added as the `_templatesIndex` constant.
  - **`template_provider.dart`** — added `templateRepositoryProvider` (DI over `appDatabaseProvider`, mirrors `packageRepositoryProvider`) and **`templateLibraryProvider`** (`FutureProvider<List<WorldSchema>>` = `[builtin asset] + listUserTemplates()`, built-in first). **`allTemplatesProvider` left untouched** (still hands the **v2** schema to world/character creation) — per the dual-stack invariant (roadmap §1.4) new worlds must keep resolving on the frozen v2 engine until the new resolver lands; the v3 library is a separate surface and the authority flip is Phase 3.11. Purely additive — no existing call site changes behavior.
  - **Scope discipline:** persistence/repository/provider only, **no UI** (the digestible unit per the prior plan). The `templates_tab.dart` rebuild (Load / Copy / Rename / Delete on the `packages_tab.dart` pattern) is the next step.
- **Next Task for Next Run:** **PR-1.4 (UI half) — rebuild `templates_tab.dart` on the `packages_tab.dart` pattern.** Wire the new `templateLibraryProvider` into the hub Templates tab: list `[built-in (read-only: View + Copy)] + user copies (Edit/Rename/Delete)`; implement `_copyTemplate()` mirroring `_copyPackage()` (`packages_tab.dart:437` — "(Copy)" suggestion, collision suffix via `TemplateRepository.nameExists`, name dialog, `ref.invalidate(templateLibraryProvider)`); rename/delete via `TemplateRepository.rename`/`delete`. Built-in tile gets read-only affordances + the "make a copy to edit" banner (roadmap §1.5). Keep it to the library tab CRUD — the responsive `TemplateEditorScreen` shell is PR-1.5. *(Also still pending, Flutter-env only: PR-1.3 closeout — run `dart run tool/export_builtin_template.dart`, commit `assets/templates/dnd5e_srd.template.json`, confirm the loader hash-assert.)*

## [10.06.2026 - 22:15] - Last Update Status
- **Current Phase:** Infrastructure (Roadmap Phase 1 — PR-1.3 Built-in template → JSON asset, RULE RESET)
- **Last Completed Task:** **PR-1.3 (infrastructure half) — Built-in template → JSON asset scaffold.** Landed everything for the v3 asset pipeline that is implementable without a Flutter toolchain (the asset *bytes* + the runtime hash-assert are confirmed in a Flutter env — see Next Task):
  - **`builtin_dnd5e_template_v3.dart`** (new) — `generateBuiltinDnd5eTemplateV3()`: the **single source of truth** consumed by both the exporter and the loader's assert, so the on-disk asset and the in-code generator can never silently diverge. It evolves `generateBuiltinDnd5eV2Schema().schema` *in place* via `copyWith`: `formatVersion → 3`, `schemaId/originalHash → builtin-dnd5e-default-**v3**` (distinct lineage so v2 and v3 worlds coexist under per-world dual-stack authority, roadmap §1.4), `version → 3.0.0`, `name → "D&D 5e (Default)"`. **`seedRows` lifted** out of the generator's typed side-channel (`BuiltinDnd5eV2Build.seedRows`) and **embedded** into the template via `liftSeedRows()` (Tier-0 lookup catalogs only — empty Tier-1/2 entries dropped to keep the asset lean). **Deterministic timestamp** (`builtinDnd5eTemplateTimestamp`) stamped so re-exports are byte-identical (idempotent; hash-excluded `createdAt/updatedAt` so drift detection is unaffected). **RULE RESET:** no field carries `rules` — left `null` (omitted via `includeIfNull:false`) so the *only* v2→v3 hash delta is the embedded `seedRows`; "zero rule-assigned fields" is the contract, the empty-vs-absent distinction is cosmetic. Old `FieldType`s kept (type swap is PR-2.3).
  - **`tool/export_builtin_template.dart`** (new) — one-shot CLI exporter (`dart run tool/export_builtin_template.dart`). Serializes the v3 template to `assets/templates/dnd5e_srd.template.json`, **canonicalized** (recursively key-sorted, matching `world_schema_hash.dart`) and **pretty-printed** (2-space) so JIT wave edits diff line-by-line (roadmap §3.1). Enforces the **rule-free invariant** (aborts with exit 2 if any field has `rules`); prints `categories`/`seedRows`/`formatVersion`/`contentHash` for cross-checking against the loader assert.
  - **`builtin_template_loader.dart`** (new, `lib/data/templates/`) — `BuiltinTemplateLoader` singleton: cached async `load()` (single in-flight load, `cachedOrNull` sync accessor, `@visibleForTesting resetCache`). On asset present → parse + **debug-only `assert`** that `computeWorldSchemaContentHash(asset) == computeWorldSchemaContentHash(generator)` (loud `FlutterError` naming the exporter if stale). On asset absent → **graceful fallback** to the generator (loud `debugPrint` in debug) so the app works in a fresh checkout before the exporter is run.
  - **`pubspec.yaml`** — registered `assets/templates/` (with an explanatory comment that the JSON is generated, not hand-authored). `assets/templates/.gitkeep` committed so the registered dir always exists.
  - **Inertness / scope discipline:** per the prior plan, **no call sites were swapped** — all 20+ `generateBuiltinDnd5eV2Schema()` consumers (template provider, bootstraps, world/package repos, import dialogs) are untouched; the loader ships as scaffold only. New code is purely additive; nothing existing changed behavior.
- **Next Task for Next Run:** **PR-1.3 (closeout, Flutter-env) → then PR-1.4 (Template library + Copy).** Two parts:
  1. *(Flutter env, one-time)* Run `dart run tool/export_builtin_template.dart`, **commit the generated `assets/templates/dnd5e_srd.template.json`**, boot the app in debug and confirm `BuiltinTemplateLoader.load()` passes the hash-equality assert (asset hash == generator hash). This is the only PR-1.3 step that needs the toolchain.
  2. *(Implementable here)* Begin **PR-1.4**: add the `templates` Drift table (one JSON blob per row: `id, name, dataJson, originalHash, currentHash, createdAt, updatedAt`) + migration, a `TemplateRepository` (list/load/save/copy/delete/rename) mirroring `PackageRepository`, and a `templateListProvider` where `allTemplatesProvider` becomes `[BuiltinTemplateLoader] + tableRows`. Copy semantics: **fresh `schemaId`, preserved `originalHash`** (lineage survives for drift detection). UI rebuild of `templates_tab.dart` follows in the same PR. Start with the table + repository (no UI) as the digestible unit.

## Todo List
- [x] Clean up legacy rule panels from the old UI *(PR-1.1 — done)*
- [ ] Implement the core Template System infrastructure *(PR-1.2 model v3 inert ✅ → PR-1.3 builtin JSON export — scaffold ✅, asset/assert pending Flutter env → PR-1.4 template library/copy: table + DAO + repository + `templateLibraryProvider` ✅, `templates_tab.dart` UI rebuild pending)*
- [ ] **Develop the mobile and desktop responsive Template Editor UI for dynamic template modification** *(PR-1.5 shell + Phase 2 components)*
- [ ] Write the Built-in SRD Template as JSON (without advanced rule fields initially, preserving static text/descriptions) *(PR-1.3, RULE RESET: rule-free; asset materialization pending `dart run tool/export_builtin_template.dart`)*
- [ ] Migrate the Built-in DnD Package (Cards/Entities), enrich descriptions using Markdown, and evolve the template dynamically with rules (Just-In-Time) *(Phase 3 waves)*
- [ ] Clean up redundant template fields, run **UI responsive layout tests**, and conduct final integration testing *(Phase 4)*

## Notes / Constraints
- Working on branch **`new-rules`**; commit every change here.
- `flutter`/`dart` toolchain is **not installed** in this remote environment, so `flutter analyze` cannot be executed here and the exporter cannot be run — changes are verified by static grep sweeps, brace/paren/bracket balance, and dependency-graph analysis. The analyze gate + the exporter run + the loader hash-assert must be confirmed in a Flutter-capable environment before release.
- **PR-1.3 asset is intentionally NOT committed yet** because it is a generated artifact (`generateBuiltinDnd5eTemplateV3().toJson()`); fabricating it by hand would risk a hash mismatch against the generator and defeat the assert. The exporter + loader + deterministic builder are all in place so a single `dart run` produces it reproducibly. The loader's graceful fallback keeps the app functional in the interim.
- Per the master roadmap, this roadmap wins over the two detail docs (notably: rule text goes into the card `description`, not a `rules_text` side-field; JIT evolution replaces a bulk ruleset drop).
- Vault is intentionally **not** updated until the whole roadmap + tests are finished (per run instructions).
