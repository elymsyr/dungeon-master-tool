"""ui/presenters — MVP presenter layer.

Each presenter mediates between a DataManager (model) and one or more
Qt view widgets, keeping business logic out of both the view class and
the host tab.
"""
